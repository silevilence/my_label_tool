# Lua 宿主原型与隔离验收

选择标准 Lua 5.4（mlua 0.12，vendored 静态编译），无需用户安装解释器。
未选择 Luau：进程上限已经覆盖 Lua 堆、Rust 快照及结果缓冲，不需要换方言获得这一约束。
mlua 内存和文本 chunk 接口依据 [官方 API](https://docs.rs/mlua/0.12.1/mlua/struct.Lua.html)。

可复跑：`cargo test --manifest-path src-tauri/script-host/Cargo.toml`。

- `vm::tests`：三个真实脚本、io/os/require/debug/dofile/load、字节码、巨量 Lua 分配、死循环、捕获命令错误仍整体失败。
- `tests/host.rs`：真实二进制通过 NDJSON 处理四图；分块大小 1 与 4 的输出完全一致。
- 相同进程测试发送 150 个 1 MiB 快照项，配置 64 MiB，在执行 Lua 前以 125 退出，验证快照也受内存限制。
- 无限 pcall 循环在 2 秒总时限以 124 退出，避免 Lua 捕获 hook 错误绕过时限。

Windows 使用 Job Object 的 ProcessMemoryLimit（提交内存）；Unix 使用 RLIMIT_AS（地址空间，包含映射）。
Rust 分配失败无额外分配地以 125 退出；Lua 堆预算为进程预算的一半，分配失败返回 MEMORY_LIMIT。
总时限覆盖收快照、执行和发结果，runner 另有独立看门狗与取消。
上限是拒绝执行/终止，绝不截断快照或部分写回。

Windows 原型上述测试通过。默认采用 256 MiB / 30 秒：64 MiB 已通过小样例，256 MiB 为解析和双份数据留余量；它不是项目规模承诺。
可配置范围 64–4096 MiB、1–3600 秒；具体可处理规模取决于标注数与脚本分配，失败时增加上限或缩小作用域。
Linux 容器（Podman / Debian bookworm）同组测试通过：六个库测试及两个真实进程测试，
包括 RLIMIT_AS 快照超限与看门狗终止，均未截断或返回部分成功。

## 桌面界面验收（2026-09-24）

使用独立应用标识 `com.mylabeltool.scriptqa` 与两张合成图片，不接触已有用户项目。
每张图片一份矩形标注，标签名为「车辆」「汽车」。真实 Tauri → runner → Lua 宿主链路：

- 默认编号脚本开启差异预览，显示两图修改、零新增/删除/跳过；第一图预览属性 sequence 为 1。
- 应用整轮结果，报告数字一致；撤销一次清空本轮历史，普通重做一次恢复全部两图。
- 通过既有保存按钮保存，检查 JSON：两图的 sequence 分别为 1、2，坐标和标签不变。
- 重新打开该目录，加载两图标注，历史为空、统计为已标注 2/2。
- 缩放至 160% 后矩形与合成图片矩形对齐，右方向键切至第二图后仍对齐。
- 预览的应用按钮和取消按钮可通过面板滚动到达；本次未新增或改绑快捷键。

## 插件契约影响核对

本次新增命令仅供宿主 UI，未暴露给插件；AnnotationShape、LabelConfig、ProjectConfig 与
插件导出/预打标协议结构未改动。共享校验提取维持既有核心约束。
manifest schemaVersion、插件协议、整体宿主 API、exporter/prelabel 能力版本均保持 1；
与任务起点比较，插件 manifest、协议和 API 版本实现无差异。不随主程序或脚本版本递增。

## 交付形态验收

`scripts/verify-script-host.ps1` 在 Windows 生产宿主和容器镜像上均通过三个真实示例：
按名称改派标签、坐标修正、跨图连续编号，每项核对两张图片的结果。
容器以非 root 身份、只读文件系统、无网络、无项目挂载运行。
桌面资源含 `script-tools/label-script-host.exe` 与 Lua/mlua 许可；容器交付相同源码编译的
独立 NDJSON 宿主。完整服务端应用仍属计划任务，普通浏览器未连接宿主时如实显示不可用。

## 完整审核范围

基点 `2dacd5dc5eb34aef87e002b4ae20cfa1f60e8610`，首轮审核终点 `f3fe3d5`。
八项实现逐项提交后，按 code-review 执行规范/需求两路独立只读审查，并按 plugin-review
核对插件边界；修复后两路复审。同期 `35f72e4` 标签样例图路线图提交不在本次审核范围。

## Standards

- **S1，阻断**：`vm.rs:63–66`（首轮终点）在失败登记前转换 Lua 参数，`pcall` 捕获转换错误后仍输出之前提交的 result/done。
  真实宿主已复现，违反 ADR 0014 任一失败整轮零写回和脚本协议的捕获错误语义。
- **S2，严重**：`commands/script.rs:50–53` 先读取配置再登记，`registry.rs:38–43` 忽略尚未登记 ID 的取消。
  立即取消可能随后仍启动宿主，违反 ADR 0013/0014 的取消终止约定。
- **S3，建议**：报告使用排序后的属性与默认帧索引，事务历史却比较原始 JSON，可能出现修改 0 却生成撤销记录。
  属语义比较重复实现的判断性建议，不是代码风格硬违规。

规范轴共 3 项，首轮最高为阻断；复审三项均闭环，无剩余阻塞。

## Spec

- **P1，严重**：同 S2。`docs/scripting.md` 要求取消即终止进程，丢失启动阶段取消不满足该行为。
- **P2，一般**：`ScriptPanel.tsx:24–38`（首轮终点）以阻塞遮罩禁止忙碌时关闭，
  不满足用户故事 17「运行期间……继续标注」。收起后需保留执行、进度、取消与旧快照保护。

需求轴共 2 项，首轮最高为严重；复审两项均闭环。作用域、整轮撤销/重做、未提交图片保留、
脚本库选项和文本读取复用通过。复审补充的逐帧导航、退出作用域及关闭草稿语义也已处理。

## 审核意见修复结果

逐项核实后执行，未更改既有规范或扩大插件契约。表中路径相对仓库根；首轮证据固定在上述审核终点。

| 编号 | 决策 | 核实与修复 | 验证 |
| --- | --- | --- | --- |
| S1 | 完全采纳 | `script-host/src/vm.rs` 改用 MultiValue，签名、参数转换及返回转换失败均登记；不让 pcall 绕过失败状态 | VM 用例及真实进程确认 failed 且零 result/done |
| S2 / P1 | 完全采纳 | `scripting/registry.rs` 保留有界提前取消记号，command 最先登记，关闭期间拒绝新登记 | 提前取消、准备阶段取消、后续新运行及退出门禁测试 |
| S3 | 完全采纳 | `annotation-utils.ts` 共享语义指纹，脚本报告与事务比较一致；普通操作比较保持原样 | 属性顺序/缺省帧索引变化不产生撤销历史 |
| P2 | 完全采纳 | 面板隐藏时保持 hooks 挂载；操作明确允许手工编辑，批量操作仍互斥；新手改使旧快照结果整体失效 | 隐藏面板后改标注再返回结果，手改不被覆盖；真实窗口可画框及取消 |
| 复审收尾 | 完全采纳 | 逐帧导航与退出作用域使用同一手工操作门禁；关闭保留草稿不再询问「放弃」 | 既有导航/作用域测试与前端全套回归 |
| L1，严重 | 完全采纳 | 主审查发现按固定图片数分块会将多个合法单图合成超过 8 MiB 的消息；runner 增加按实际序列化字节拆分 | 12 MiB 快照在分块 1 与 16 下输出一致 |
| L2，一般 | 完全采纳 | 输入断管可能掩盖宿主内存上限退出；等待最终错误/退出并优先识别 125 | 输入期间模拟内存终止仍报告 MEMORY_LIMIT |
| L3，一般 | 完全采纳 | runner 原先 hello 后直接发送快照；改为确认 ready/version 后放行 writer，取消和 EOF 均释放等待 | runner 正常、超时、取消、崩溃、行上限回归 |
| L4，一般 | 完全采纳 | 已存在生产资源时开发模式可能选中旧宿主；debug 模式优先独立 crate 的 debug 产物 | 开发构建与最终生产打包检查 |
| L5，一般 | 完全采纳 | Lua 空表同时表示 map/array；submit.annotations 空表按明确数组契约归一化为 [] | 真实 Lua 清空图片标注测试 |
| 工具检查 | 完全采纳 | 独立 crate 的 all-targets Clippy 要求测试模块置后，转换错误旁观改用 inspect_err | 独立宿主 Clippy 使用 -D warnings |

## 插件正式审查

结论：通过，阻断 0 / 严重 0 / 一般 0 / 建议 0（仅插件边界维度）。

| 维度 | 结果与证据 | 约束引用 |
| --- | --- | --- |
| 契约与 Schema | 通过；核心 annotation/export/prelabel 类型、ProjectConfig 结构与插件 Schema 无改动，脚本类型/协议独立 | AGENTS.md §10 契约先行、边界影响评估；ADR 0013–0015 |
| ID 与版本分层 | 通过；`src-tauri/src/plugins/versioning.rs:4` 起各版本仍为 1，插件稳定 ID 未变 | AGENTS.md §10 稳定 ID、版本分层；ADR 0007 |
| 能力声明 | 通过；插件 manifest/握手未修改，脚本命令注册表不进入插件能力面 | AGENTS.md §10 能力声明；ADR 0003、0004、0015 |
| 向后兼容 | 通过；未删除或改变插件字段、错误码及目标版本行为 | AGENTS.md §10 向后兼容；ADR 0006、0007 |
| 权限与隔离 | 通过；插件 AppContainer 与代理未变，Lua 安全库/文本 chunk/进程限制按独立信任边界实现 | AGENTS.md §10 权限最小化、故障隔离；ADR 0005、0013 |
| 目录与工程约定 | 通过；新命令经 tauri-api 封装，脚本文案集中 i18n，脚本宿主独立 crate，不混入插件目录 | AGENTS.md §6、§10 目录归属；ADR 0015 |
| 测试 | 通过；插件 conformance 全量回归，新增宿主与 runner 测试独立覆盖协议、取消与资源上限 | AGENTS.md §7、§10；ADR 0003、0004 |

未发现需修改约束的规范缺陷。

## 最终交互补验

长循环运行后收起面板，真实画布新增一份矩形，随后通过全局操作卡取消。
宿主进程退出，矩形保留；保存 JSON 后第一图有两份图形、第二图一份，原编号仍为 1、2。
自动化另覆盖执行期间手工编辑使快照失效时，不覆盖手工结果。

## 最终检查结果

- `npm run typecheck`、`npm run lint`：通过。
- `npm run test:coverage`：97 个文件、589 项通过；行覆盖率 96.09%，分支覆盖率 91.62%。
- 根 crate `cargo clippy`：通过；`cargo test -- --test-threads=1`：263 个库测试、1 个插件协议集成测试、4 个脚本 runner 测试通过。
  原有 14 个依赖外部 ONNX/Runtime、GPU、FFmpeg 或回收站交互的 ignored 测试未在本轮启用；本次不修改其实现。
- 独立宿主：8 个库测试、3 个真实进程测试通过；`cargo clippy --all-targets -- -D warnings` 通过。
- Linux 容器重建后同组测试通过，三个真实示例再次通过；最终桌面资源包含约 1 MiB 的静态 Lua 宿主及许可证。
- `npm run tauri build -- --bundles nsis --config '{"bundle":{"createUpdaterArtifacts":false}}'` 用于本地安装包验收；没有推送、发版或更新版本号。

规范轴 3 项、需求轴 2 项（取消问题在两轴各计一次）均已修复并复审；无剩余阻断项。
