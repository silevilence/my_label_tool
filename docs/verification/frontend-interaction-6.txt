前端交互⑥：开发、快速审核与修复
frameSummaries 按名称仅在模型入口匹配路径；index 是可用抽帧序，frameIndex 是源视频帧号。各视图按不可变输入共享弱引用缓存摘要。
useVideoFrameNavigation 统一路径/序号跳转、步进与边界，并显式进入可退出的视频作用域。
时间轴、侧栏使用同一摘要显示已标注与关键帧；插值模型及面板共享映射与关键帧判定（video-keyframes 从既有插值语义提取）。
缺失路径从浏览序列排除，但插值拒绝不完整序列，避免静默漏生成。
快速审核：移除 selectedName ↔ images 的往返查找；时间轴资源门禁覆盖所有项目写操作；确认无新增业务契约/协议语义。
测试：空/单帧/无标注/部分标注/稀疏关键帧、乱序与缺失路径、首尾边界、非法目标、侧栏与时间轴分布一致、scope 导航集成。
验证：typecheck、lint、cargo clippy 通过；60 文件 358 测试通过，行覆盖率 95.35%。
插件审查七维：契约/Schema、ID/版本、能力协商、兼容、权限隔离均未变；目录工程、测试通过。无阻塞项。
