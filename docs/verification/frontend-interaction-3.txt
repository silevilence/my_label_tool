前端交互③：开发 / 快速审核 / 修复记录
需求：新增 useOperations，统一资源互斥、独立进度/消息、取消和终态；接入保存、插件导出、预打标、视频导入/批量/重抽帧、模型/运行时下载、图片删除及更新安装。
同资源 begin 同步拒绝；取消请求不提前释放资源；失败/成功消息分别归属操作；取消失败允许重试。
Q1（Important）：忙碌选择器引用尚未赋值的操作 ref 时可漏掉首次刷新。完全采纳，订阅操作列表后在渲染中读取句柄，并新增运行中的视频导入验收。
Q2（Important）：异步插件刷新错误可能归入后续导出。完全采纳，分离操作失败与独立刷新错误。
Q3（Important）：旧取消入口在终态仍调用后端。完全采纳，注册表拒绝终态/重复取消，视频测试更新并新增运行中取消测试。
更新取消：Tauri API 无传输中断，下载后不安装/不重启，并保留资源至下载结束；单测明确验证。
验证：typecheck / lint / cargo clippy 通过；54 文件 345 测试通过，行覆盖率 94.98%，useOperations 行覆盖率 100%。
复跑：npm run test:coverage -- --maxWorkers=4。
插件正式审查：契约与 Schema、ID 与版本、能力声明、向后兼容、权限与隔离、目录约定、测试七个维度均通过（AGENTS.md §10 / ADR 0003–0006）。
插件导出仍通过原有代理，按 supportsCancel/supportsProgress 适配；请求结果字段、权限和协议均未修改。
结论：阻断 0 / 严重 0；可以原地勾选并提交。
