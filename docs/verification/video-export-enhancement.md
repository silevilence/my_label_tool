# 视频标注导出增强验收记录

## 默认抽帧设置

完成：设置面板新增项目默认间隔，保存在项目根目录的宿主设置文件；默认 30，支持 1–1000000 的整数。
首次标注导出前可保存，重开恢复；单次抽帧弹窗预填默认值，旧项目及其它项目不受影响。
快速审核通过：修复设置尚未加载时提前打开抽帧弹窗导致预填旧默认值的问题；写入失败不更新已保存状态，旧项目的异步读取不会覆盖新项目。

验证：完整前端 293 项通过、行覆盖率 94.56%；补充 Schema 与解析器一致性用例。
typecheck、lint、cargo clippy 通过；未改 Rust 逻辑。自动化覆盖默认值、边界、配置格式、保存恢复、项目切换及写入失败。
插件边界预审：宿主 settings schemaVersion 1 独立于 ProjectConfig（后者依赖标注文件路径），不传给插件；
标注、导出、预打标、ID、API/协议、能力、权限、配置迁移均未变化，文案与默认值按约定集中。

按用户要求不使用 Computer Use，不执行实际桌面标注验收。

## 批量抽取未准备视频

完成项目菜单入口、串行队列、逐项进度/失败详情与取消。仅选择未抽帧视频，使用当前项目默认值；
结果逐个合并，保留原图片、已准备视频与标注，失败继续处理其它项，取消停止后续项。
快速审核通过：回调使用最新素材快照，避免 React 合批渲染时覆盖早先结果；批量弹窗阻止画布快捷键与保存。
完整 App 自动化覆盖菜单到素材列表变化，队列测试覆盖失败继续、取消与结果保留。
完整前端 300 项通过，行覆盖率 94.62%；typecheck、lint、cargo clippy 通过，无 Rust 逻辑或插件契约变化。

## 右键重新抽帧

完成：视频条目右键入口，复用 DeleteImageDialog 的 3 秒等待、鼠标确认与红色后果说明。
新帧先生成到独立目录，再暂存并回收旧元数据明确列出的文件；移动或回收失败恢复旧文件。
项目根目录/其它文件不删除；成功后批量清除旧帧标注与相关撤销/重做，避免逐帧复制整个 store。
旧标注快照因帧目录变化不会误关联到新帧；解码缓存随旧素材移除而失效。

快速审核修复：Windows `\\?\` / UNC 路径规范化，避免 Rust canonicalize 返回值造成相对帧名错误和重开匹配失败；
素材列表记住的旧帧路径失效时回到新首帧。阻止在保存、预打标或插件导出期间重抽。
前端 304 项通过，行覆盖率 94.63%；typecheck、lint、clippy 通过。
Rust 230 项通过、13 项隔离，插件 conformance 1 项通过。
6 项后端用例覆盖抽帧失败、移动失败、回收失败回滚、范围/来源校验、元数据变化、旧版根目录及其它文件保留。
另显式执行 `cargo test --manifest-path src-tauri/Cargo.toml real_ffmpeg_reextract_recycles_old_frame_directory -- --ignored --test-threads=1` 通过：
生成临时 64×48 / 10 帧视频，先按间隔 3 抽取 4 帧，再按间隔 5 得到源帧 0/5；真实回收旧帧目录并重载新帧。
测试不启动 GUI。新增 reextract_video 为宿主专用命令，复用既有返回结构，无插件入口或协议变更。

## 按抽帧图片导出 YOLO txt

完成：含视频项目的导出面板增加独立 YOLO txt 入口；沿用逐帧导出器，每帧对应一个同名文件，
保留视频子目录以隔离同名帧，空帧为空 txt，另附 classes.txt；普通图片同时导出。
界面明确显示帧数及未准备视频数量，不修改项目 JSON 保存配置。选择目录取消时不写文件。
快速审核补强：拒绝未知标签、无效/越界矩形、无效图片尺寸和输出名冲突；非矩形继续明确拒绝，避免静默丢标注。
集成测试覆盖 1 张图片 + 2 个不同尺寸视频 + 空帧，逐个核对路径、类别和归一化坐标，证明视频导出不重新解码全部帧。
完整前端 316 项通过，行覆盖率 94.67%；YOLO 导出器行覆盖率 100%、分支 92%。
typecheck、lint、clippy、前端生产构建通过。本项没有更改 Rust 或插件契约。
