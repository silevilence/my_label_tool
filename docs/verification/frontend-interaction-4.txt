前端交互④：开发、快速审核与修复
选择、作用域、图片列表由 useAnnotationStore 统一持有；removeImages 单次更新清理帧信息、标注、历史并计算邻图。
搜索确认建立搜索作用域；视频入口建立视频作用域；作用域条可退出；行组件负责滚动。
旧视频测试要求恢复影子游标，与本次明确移除要求冲突，改为首帧行为并继续验证重绑步进。
新增覆盖：搜索/视频 push/pop、空范围、未标注导航、缺失路径、末张/当前张/全部删除、单次订阅通知、行滚动及退出。
插件审查七维：未改变对外数据结构、Schema、ID/API/协议版本、能力协商、权限或隔离；目录及类型约束满足；宿主 store 行为测试通过。
AGENTS §6 修订当前编辑对象归属；项目插件技能未记载该状态划分，无需契约同步。
验证：typecheck、lint、cargo clippy 通过；56 文件 351 测试通过，行覆盖率 95.13%。无阻塞项。
