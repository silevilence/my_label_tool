前端交互②：开发 / 快速审核 / 修复记录
需求：动作元数据表、固定键、行为注册、统一解析与三处共用的冲突检测均已实现。
质量：blocking 独占；light 允许 global；canvas/global 按优先级与稳定 ID 决胜；标签最低；不可用动作不消费按键。
Q1（Important）：固定行虽然不可点击，配置 hook 仍可修改固定动作。完全采纳，hook 增加元数据约束。
Q2（Minor）：历史配置大写单键在冲突检测和执行时不一致。完全采纳，统一标准化。
规范：纯逻辑与注册状态分别位于 lib / store；新增文案集中 i18n；不新增依赖和外部契约。
验证：typecheck / lint / cargo clippy 通过；52 文件 339 测试，行覆盖率 94.82%。
复跑：npm run test:coverage -- --maxWorkers=4。
结论：无阻塞项，可以原地勾选并提交。
