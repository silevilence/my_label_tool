前端交互⑤：开发、快速审核与修复
resolveGesture 统一 Stage 和三类图形选择/拖动意图；中键 cancel、Ctrl+右键 pan、未知按钮 null。
useDraftGesture 统一矩形、多边形、关键点、平移；Esc 与中键均取消；提交同步读取最新草稿，避免单击点因 React 批处理丢失。
快速审核修复：修饰键模式切换保留多边形顶点，清理游标；工具切换取消草稿。坐标往返使用浮点近似断言。
createTransform 供几何、拖拽/变换、缩放、多边形草稿和插值预览共享，包含缩放锚点不变量测试。
验证：typecheck、lint、cargo clippy；58 文件 355 测试通过，行覆盖率 95.25%。分类矩阵、状态转移、矩形/多边形 Esc、遮罩门禁测试通过。
浏览器实测：临时页面运行真实 App/React/Konva，只替换宿主文件 I/O，使用 640x480 网格图。
矩形拖绘可见；缩放前后标注与原图网格对齐；左右切图后标注恢复；多边形两点草稿 Esc 消失；工具快捷键立即生效。
矩形按住鼠标时 Esc 由真实键盘 hook 的组件测试覆盖（浏览器工具不支持跨调用保持鼠标按下）。
快捷键改绑与遮罩阻断沿用 entries 集成测试；导出标准格式解析由现有导出器测试验证，未冒充 Windows 原生端到端导出验收。
插件审查七维：标注原图像素契约不变；Schema、ID/版本、协商、兼容、权限隔离不变；目录/工程与测试满足，无阻塞项。
临时浏览器 fixture 已移除，不进入产品或提交。
