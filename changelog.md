# Change Log

## V0.9.1

### ✨ 新功能

- 预打标设置新增「模型结构查看」：可离线查看 ONNX 模型的网络结构，包括节点与数据流向可视化、算子分布统计、opset 版本以及输入输出张量形状的保守推导结果，便于接入模型前评估其结构是否合适
- 新增标注框尺寸显示：矩形标注框内可直接显示其在原图中的像素宽高，缩放画布时数值保持稳定；是否显示可按默认、选择、标注三种模式独立开关，默认仅标注模式显示

### 🐛 Bug 修复

- 修复预打标模型来源无法直接选择模型库中任意模型的问题；现在选择任意模型都会保持类别映射与批量任务上下文一致
- 修复预打标设置与执行中的一系列反馈问题：点击重试时按最新表单配置执行，模型下载进度与本地表单编辑互不干扰，任务取消确认不再与模型列表刷新冲突，出现资源冲突时不再丢失原始失败原因，同一任务的完成提示不再重复显示

## V0.9.0

### ✨ 新功能

- 新增视频标注：同一项目可同时包含图片和多个视频，左侧素材列表统一选择；视频在本地抽帧为原分辨率 PNG 后复用现有画布与标注操作，全程离线，无需额外安装组件（解码工具随应用分发，安装包体积相应增大）
- 视频导入可选按 FPS（默认 5）或固定帧间隔抽帧，可随时取消，失败或取消会清理本次未完成的内容且不覆盖原视频；抽帧方式作为项目设置保存，可一键批量准备项目中所有未抽帧的视频，也可对单个视频重新抽帧，重抽前按删除确认流程提示将移除的旧帧与标注数量
- 时间轴与逐帧浏览：选中视频帧后画布底部显示时间轴，可拖动定位，并显示当前源帧号、时间戳与抽取帧总数；`PageUp` / `PageDown` 逐帧切换（可在快捷键设置中改绑），在不同视频之间切换会记住各自停在哪一帧
- 跨帧插值：将同一目标在不同帧上的标注设为关键帧后，可先预览中间帧的插值结果（黄色虚线，不修改标注），逐帧确认后再应用，支持矩形、关键点和顶点结构相同的多边形，应用结果可撤销重做
- 视频导出：项目 JSON 会保存时间戳、源帧号与轨迹属性，重新打开项目可完整恢复；YOLO / COCO / VOC 可按各自格式导出逐帧数据集，格式下拉框统一决定「保存 / 另存为」与 `Ctrl+S` 的输出
- 新增「预打标资源限制」设置：可配置预打标的最大内存占用（默认 80 MiB）与原始候选框数量上限（默认 100000），超出时给出明确提示而不是直接失败
- 新增操作状态区：预打标、导出、视频抽帧、模型与运行时下载、保存等操作各自显示进度与取消入口，可同时进行；失败信息按所属操作常驻显示、可单独关闭，导出失败可在面板内就地重试
- 快捷键设置改版：区分可改绑动作与固定键（`Ctrl+Z` / `Ctrl+Y` / `Ctrl+S` 等以固定行展示、不可改绑），冲突原因就地说明，并新增单动作与全部的「恢复默认」入口（恢复前需确认）
- 新增草稿标签防护：仅存在于未保存草稿中的标签不会被选为新框使用的标签；取消标签修改时若有标注引用了已删除的标签会明确提示，标注不再静默套用其它标签的名称与颜色

### 🚀 优化

- 素材列表改版：图片行显示后台生成并缓存的缩略图，列表按可视区域渲染，上万张图片的项目也能流畅滚动；打开大目录的扫描移到后台，不再卡住界面
- 画布操作更顺手：角落常驻显示缩放百分比（点击即恢复「适应」），空格键或鼠标中键拖动即可平移，滚轮在图片外的灰色区域也能缩放并自动取最近的图片边界点
- 界面不再出现系统级与浏览器原生弹窗：确认与命名输入统一为应用内弹窗，可用 `Esc` 取消；开启安全模式、停用插件、清除失败等破坏性操作补充后果说明，危险按钮不再只在悬停时变红
- 进度反馈更真实：没有总量时显示动画而不是固定百分比，批量抽帧显示逐项进度（完成 / 失败 / 剩余），侧栏进度条与相邻文字含义一致
- 错误提示按所属操作展示，不再互相覆盖或对同一次失败重复提示，屏幕阅读器可感知错误出现
- 新建与编辑模型的表单逐字段显示不合格原因并在字段上标注，键盘填完可 `Enter` 提交；PT 转换失败可就地重试，无需返回重来
- 搜索结果为常驻可见、可用 `Esc` 退出的作用域（退出后回到项目顺序）；搜索浮窗支持点击背板关闭，图片列表右键菜单在轻微滚动时保持打开并显示「删除图片 F8」这类快捷键提示
- 可访问性与文案清理：进度条补充语义、暂不可用的菜单项改为禁用并说明原因、作用域变化会被朗读；关闭项目设置或类名映射时不再静默丢弃未保存的修改

## V0.8.0

### ✨ 新功能

- 新增图片删除功能：在图片列表条目或画布上右键选择「删除图片」，也可用快捷键（默认 `F8`）删除当前图片；删除的是磁盘上的源文件，会移入系统回收站，可从回收站恢复
- 删除前弹出防误触确认：显示文件名、完整路径与该图片已有的标注数量，等待 3 秒倒计时结束后才能确认，且只接受鼠标点击；弹窗打开期间不会误触画布快捷键
- 删除图片会同步清除该图片的标注与撤销 / 重做记录，图片本身可从回收站恢复但标注无法撤销回来；删除当前图片后自动切换到相邻图片，删除其他图片时画布与当前图片保持不变
- 删除快捷键可在快捷键设置中改绑，并校验与标签键、固定键的冲突；旧配置已占用默认键位时保持未绑定，不会顶替已有快捷键
- 保存、导出或预打标进行中时禁止删除并给出提示，避免文件与数据状态不一致

## V0.7.0

### ✨ 新功能

- 预打标模型支持配置更新地址：编辑模型时可填写模型文件下载直链，点击「更新模型」即可下载并校验后替换当前模型文件，下载进度实时显示且可随时取消；新文件保存到应用数据目录，不会覆盖原始模型文件
- 打开包含 YOLO 标注的图片目录时自动检测并提示创建项目配置，无需手动导入；自动读取标注中的标签并与现有标签按名称合并，同名标签保留当前的颜色与快捷键等设置，已标注内容的标签引用同步正确

## V0.6.2

### 🐛 Bug 修复

- 修复打开包含项目配置的图片目录后，标签设置仍显示「通用目标检测」并可能使用全局标签覆盖项目标签的问题；现在会正确保留并加载项目专用标签模板

## V0.6.1

### 🐛 Bug 修复

- 修复低分辨率屏幕下部分设置弹窗（插件权限确认、预打标执行、预打标设置、快捷键设置）超出视口导致关闭按钮不可见的问题，弹窗内容现会限制高度并可滚动

## V0.6.0

### ✨ 新功能

- 插件系统（实验性）：可通过安装 ZIP 插件包扩展标注工具能力，支持标签预设（提供新标签模板）、自定义导出格式与外部预打标三种扩展类型；安装时逐项确认文件与网络权限，插件可启用、停用、更新或卸载，连续失败自动停用并保留诊断日志；内置安全模式可禁止代码型插件运行
- 新增插件 SDK 与开发工具，第三方可按清单契约开发上述三类插件
- 插件配置支持版本化与自动迁移：插件升级后旧配置按迁移脚本自动升级，无需重新设置
- 预打标 ONNX Runtime 下载支持取消，下载过程中实时显示进度与文件名
- 预打标推理可选启用 DirectML GPU 加速，当前运行时是否支持 GPU 加速在设置中明确展示
- 预打标来源新增「插件」：可在模型库中选择已安装的预打标插件执行推理，与内置模型并列展示
- 批量预打标支持取消单张图片的推理，已完成的图片结果保留

### 🚀 优化

- 应用启动改善：非首屏模块按需加载，启动白屏时间缩短

## V0.5.1

### ✨ 新功能

- PT 模型转换改版：新增 uvx 转换方式，无需本地安装 Python 与 Ultralytics 环境（首次运行会联网按需下载依赖并提前提示）；可配置导出输入尺寸与简化选项，转换前预览将执行的命令，过程中实时显示输出，可随时取消
- 应用图标更换为新版设计

### 🚀 优化

- 预打标执行改版为浮窗：侧边栏只保留单一入口按钮，运行进度与结果展示更清晰

### 🐛 Bug 修复

- 修复从仅含类别文件、没有标注文件的 YOLO 目录创建项目时报错的问题

## V0.5.0

### ✨ 新功能

- 新增 AI 预打标：导入 YOLO 模型（ONNX）后，可对当前图片或批量图片自动生成标注框
- 新增模型库：支持添加 ONNX 模型与 PyTorch 的 `.pt` 模型，`.pt` 模型可一键转换为 ONNX 并校验；自动识别模型类别数与输入尺寸
- ONNX Runtime 按需安装：可一键下载（自动校验完整性）或手动指定 DLL，不随安装包分发
- 模型类别与项目标签映射：名称一致时自动匹配，也支持手动绑定已有标签、从类名新建标签或排除类别；未映射的类别运行时自动跳过
- 批量预打标默认跳过已有标注的图片，可开启强制覆盖；任务可随时中断，每张图片的结果可独立撤销
- 图片搜索升级为表达式搜索：支持按文件名、标注标签、类别和文件大小组合过滤，支持 `and` / `or` / `not` 布尔语法；输入时提供语法高亮与自动补全，表达式无效时保留原有搜索结果

## V0.4.0

### ✨ 新功能

- 新增多边形标注工具：适用于不规则形状的目标，逐个点击添加顶点，双击完成绘制；绘制过程中显示跟随鼠标的预览连线，按 `Backspace` 可撤销最近一个顶点
- 新增关键点标注工具：可在图片上标记单个关键点位置，适用于特征点定位等场景
- 顶部新增标注工具切换，支持矩形 / 多边形 / 关键点三种图形，默认快捷键 `R` / `P` / `K`
- 标签新增形状类型设置：可为每个标签指定适用的图形（通用、矩形、多边形、关键点），切换工具时自动选中兼容的标签

### 🐛 Bug 修复

- 修复「另存为」标签模板后项目配置未同步为新模板的问题
- 修复图片列表区域高度被标签设置挤压、显示空间不足的问题

## V0.3.1

### ✨ 新功能

- 图片列表新增搜索跳转功能：按 `Ctrl+F` 或点击图片列表右上角的放大镜按钮打开搜索浮窗，可按文件名进行普通文本或正则表达式搜索；结果实时更新并显示小图预览，单击选中、再次点击或按 `Enter` 才会切换图片，避免误操作；切换后自动复用现有切图逻辑，保证当前图片标注正确保存、目标图片标注正确加载
- 项目专用标签模板保存时新增变更保护：保存前会分析标签的新增、删除、重排、重命名等变化并展示影响摘要；删除已被使用的标签需二次确认，避免标注被静默改错或丢失；新增「保存并更新标注」按钮，可一键按映射表同步已有标注的标签引用，不会出现 A/B 互换时的连环替换问题

### 🚀 优化

- 错误提示改为顶部浮窗显示，不再占用侧边栏空间或遮挡画布操作

## V0.3.0

### ✨ 新功能

- 应用启动后自动检查更新，也可在菜单中手动「检查更新」；发现新版本时显示当前版本、新版本与发布说明，确认后自动下载安装并显示进度，安装完成自动重启
- 绘制标注框时鼠标显示十字光标，并显示随鼠标移动的水平/垂直辅助线，方便精确对齐定位；两者均可在设置中单独开关
- 右上角标签快捷键列表会高亮当前新建标注框将使用的标签，便于快速确认默认绘制标签

## V0.2.1

### ✨ 新功能

- 按 `Ctrl+S` 可快速保存当前项目，无需再通过导出面板操作
- 画布默认模式下可显示标签快捷键速查面板（如 `1：人`、`Q：车`），方便记忆各标签对应的按键
- 设置中新增画布提示开关，可分别控制是否显示模式操作提示与标签快捷键速查
- 设置中新增标签名显示配置，可分别控制默认、选择、标注三种模式下是否在标注框上显示标签名
- 切换图片时后台预加载下一张图片，再次切换时可直接复用，减少空白等待

### 🚀 优化

- 连续打标时，画完一个框不再自动选中它，此时按标签快捷键只切换后续新框的默认标签，避免误改刚画完的框；主动选中已有框后仍可按快捷键修改其标签
- 切换默认标签时，画布上方会短暂显示当前标签名与颜色，方便快速确认
- 切换大图时显示加载进度反馈，不再像卡住一样无响应
- 保存项目时显示进度条与「保存完成」提示，保存状态更明确
- 模式操作提示补充了 `Shift` / `Ctrl` 修饰键、`Ctrl+S` 保存等说明，上手更轻松

## V0.2.0

### ✨ 新功能

- 从外部 YOLO 数据集创建项目：手上只有 YOLO 格式的图片和标注目录时，无需本工具项目文件即可直接导入并生成项目；自动读取 `classes.txt` 生成标签（无则按类别序号命名，如 `class_0`），导入后保存项目配置，再次打开该目录自动按本项目加载；导入完成后给出缺失图片、缺失标注与异常标注行的数量摘要
- 一键跳转下一张 / 上一张未标注图片，快速定位待处理内容
- 标注进度统计：实时显示已标注、未标注与总图片数量，并附完成进度条

### 🚀 优化

- 标注框缩放支持自由调整宽高比，不再锁定比例，可独立改变宽和高
- 标签与模板管理整合为弹窗，主界面更清爽

## V0.1.0

### ✨ 新功能

**图片浏览**

- 打开本地图片文件夹即可开始标注，支持 JPG / PNG / BMP 格式，自动跳过损坏或空文件并给出提示
- 方向键快速切换上一张 / 下一张图片
- 滚轮以光标位置为中心缩放画布，Ctrl + 滚轮微调；Ctrl + 右键或中键拖动平移画布

**矩形框标注**

- 在图片上绘制矩形标注框，支持选中、拖拽移动、四角缩放调整
- 按住 Shift 强制进入选择模式，按住 Ctrl 强制进入标注模式，无需来回切换工具
- 针对大图片优化了标注与渲染流畅度

**标签体系**

- 内置「通用目标检测」与「道路交通」两套标签模板，开箱即用
- 自由增删改标签：名称、颜色、单键快捷键
- 标签模板管理：新建、保存、另存为、删除自定义模板（内置模板不可删除）
- 按下标签快捷键（如 1 / 2 / 3）快速切换当前标签，已有选中框同步切换其标签

**导入与导出**

- 导出标注：原生 JSON、COCO JSON、VOC XML、YOLO TXT、自定义字段映射 JSON 五种格式
- 导入标注：支持原生 JSON、COCO、VOC、YOLO 标注文件
- 打开图片文件夹时若检测到项目配置文件，提示自动加载

**操作体验**

- 撤销 / 重做：标注的新增、删除、移动、调整均可回退（默认 Ctrl + Z / Ctrl + Y）
- 选中标注框按 Delete 删除；提供「清空当前图片所有标注」按钮，需二次确认
- 通用快捷键配置面板：切图、缩放等快捷键可自定义并持久化保存

**应用特性**

- 离线运行、无需联网、无需安装额外依赖的 Windows 桌面应用
