# 🗺️ Product Roadmap

## 📝 计划中 (Planned)

- [ ] **实现 custom 格式反向导入**
  - [ ] 基于用户自定义字段映射解析标注文件，转换为内部 `AnnotationExport`
  - [ ] 提供导入前字段映射预览与样例校验，避免错误映射静默污染标注数据
  - [ ] 导入后复用项目专用模板机制，标签快照不污染全局模板

- [ ] **明确内嵌 MCP HTTP 接口契约与安全边界**
  - [ ] 采用 MCP Streamable HTTP 作为外部接入协议，服务随桌面应用启动和退出，默认仅允许本机回环地址访问
  - [ ] 明确第一版工具范围、输入输出结构、错误码、长任务反馈方式以及新旧 MCP 客户端兼容范围
  - [ ] 明确鉴权、来源校验、项目目录访问边界、危险操作确认、调用审计与请求限额要求，禁止直接无差别暴露现有 Tauri commands
  - 验收：形成可用于实现和测试的接口清单、安全约束与兼容性验收清单

- [ ] **实现软件内嵌 MCP Streamable HTTP 服务与连接管理**
  - [ ] 提供 MCP 服务启停、监听地址、端口、鉴权凭据轮换和连接状态展示
  - [ ] 服务启动失败、端口冲突、客户端断连及应用退出时有明确反馈并能正确清理资源
  - [ ] 外部客户端可以完成服务发现、工具列表查询和应用状态读取，未授权请求不能调用工具
  - 验收：至少使用两类目标 MCP 客户端完成本机连接测试，服务启停和异常恢复不会影响正常人工标注

- [ ] **实现人工与 MCP 工具的互斥操作控制权**
  - [ ] 建立可观察的人工操作、MCP 申请、MCP 操作和工具锁定等状态，并在界面中持续显示当前控制方
  - [ ] MCP 获得操作控制权后，暂停会产生冲突的人工写操作，但用户仍可查看状态并随时主动收回人工控制权
  - [ ] 用户切回人工操作后，当前 MCP 操作权限立即失效；工具后续写操作必须重新申请控制权
  - [ ] 用户可按指定时长锁定工具；锁定期间拒绝新的 MCP 操作申请，并显示剩余锁定时间
  - [ ] 工具请求被拒绝、控制权被收回、锁定生效或状态已变化时，返回外部客户端可识别的明确结果
  - [ ] 实现前进一步确认初始状态、申请是否需要人工批准、控制租约超时、断连释放、锁定粒度及只读请求是否受限
  - 验收：人工与 MCP 不能同时执行冲突写操作；人工收回、限时锁定、锁定到期和 MCP 重新申请的完整状态流转可重复验证

- [ ] **实现 MCP 标注操作与界面状态实时同步**
  - [ ] 提供当前项目、图片、标签和标注的查询能力，外部参数使用项目内稳定标识而非任意绝对路径
  - [ ] 提供新增、更新、删除和批量写入标注的领域操作，继续遵守原图像素坐标及标签图形类型约束
  - [ ] 所有 MCP 写操作复用现有标注状态与撤销/重做语义，操作成功后画布、图片列表和统计信息立即同步
  - [ ] 为状态变更引入版本校验和原子批处理，检测人工状态变化、过期请求或并发覆盖并返回冲突结果
  - 验收：外部工具完成一轮标注增删改后，界面显示、撤销/重做和导出结果一致；过期或越权写入不会污染项目状态

- [ ] **扩展 MCP 项目操作并完成安全与兼容性验收**
  - [ ] 按权限逐步开放项目保存、格式导出和预打标任务，长任务支持进度查询、取消及可理解的失败结果
  - [ ] 覆盖鉴权失败、非法路径、非法坐标、控制权冲突、工具锁定、断连、超时和应用退出等测试场景
  - [ ] 补充 MCP 连接配置、工具说明、安全提示和故障排查文档
  - 验收：外部客户端可在控制权规则下完成“读取项目—修改标注—保存或导出”流程；安全测试、自动化测试和手动验收清单全部通过

- [ ] **实现受控环境下的宿主文本读取适配器**
  - [ ] 提供「原生 / Python / 命令」三种读取模式，作为宿主读取文本文件内容的统一入口；模式配置保存在机器级设置，不写入项目文件
  - [ ] 读取模式易于扩展：模式注册表集中声明每种模式的名称、所需配置字段与自检方式，界面与配置校验由注册表驱动，不逐模式硬编码
    - 任务描述：适配器接口是文本读取的唯一变化点；新增模式只新增一个实现与它的配置元数据，不改动调用方、不引入第二条读取路径
  - [ ] 命令模式以「可执行文件 + 参数数组」配置，不经 shell 解析；解释器路径与命令在执行前可预览
  - [ ] 读取接口支持一次读取多个文件，导入大量标签文件时不得按文件数重复启动外部程序
  - [ ] 提供读取自检：宿主写出含中文与多字节字符的临时文本文件，再用当前适配器读回比对，配置时与会话首次读取时各执行一次
  - [ ] 外部程序探测、执行、超时与取消复用现有 .pt 转换的宿主外部进程模式（命令预览、超时、进程组终止、可取消）
  - 验收：非受控机器上默认原生模式与现状行为一致；受控机器上三种模式均可读到明文标签文件；一次导入中外部程序启动次数不随文件数增长；自检结果在设置中可见；临时新增一个演示模式时只需改动注册表与一个实现文件，现有读取调用点零改动（验证后删除该演示模式）

- [ ] **把宿主全部文本读路径接入读取适配器**
  - [ ] 覆盖标签文件（txt/xml/json）、项目配置与导入来源文件，以及应用数据目录下的标签、模板、快捷键、模型库与资源限制配置
  - [ ] 写入侧保持原生，并在界面与文档中说明受控环境下「写入后回读校验」不成立，保存成败以写入调用返回为准
  - [ ] 插件经宿主文件代理读取项目文件时自动获得明文，不新增插件能力、不修改插件契约与版本面
  - 验收：受控机器上打开项目、恢复标签、导入 YOLO/VOC/COCO、保存项目配置全流程可用；插件预打标与插件导出读取项目文件不受加密影响；插件 manifest schema、宿主 API 与协议版本均保持 v1

- [ ] **读取适配器失败语义与用户反馈**
  - [ ] 适配器不可用（自检失败、程序缺失、超时、输出非法 UTF-8）时明确阻断需要读取文本的操作，禁止静默回退原生读取
  - [ ] 失败信息按操作归属展示，并给出可执行的修复指引（检查模式配置、解释器路径、DLP 策略是否收紧）
  - [ ] 设置中提供模式切换、重新自检与最近一次自检结果
  - [ ] 用户文档说明三种模式的适用条件、配置步骤、自检失败的排障路径与不建议的配置；核实清单（docs/verification/dlp-encrypted-text-read-adapter.md）作为使用者在自有受控环境中的自查依据，不作为开发侧前置条件
  - 验收：程序缺失、输出乱码、超时三种情况下均有明确错误且标注数据不被写入；修复配置后无需重启即可重新读取

- [ ] **实现服务端形态的前端 HostBridge 抽象层**
  - [ ] 把 `invoke`、图片 URL、进度事件、目录选择、更新与进程重启五个原语收敛为可替换的桥接接口，桌面实现与 HTTP 实现分别落地
  - [ ] 桌面实现与现状行为逐字节一致；浏览器端不再出现 Tauri API 直接引用（`updater.ts`、`useAppUpdate.ts` 一并收口）
  - 验收：桌面形态下现有测试与手动清单全通过；只提供 HTTP 后端的宿主下能完成打开项目、加载图片、绘制标注、保存

- [ ] **验证服务端桥接层的传输可行性**
  - [ ] 核实脱离 webview 驱动命令层时 capabilities/ACL 是否仍生效，或必须改由 HTTP 层直接调用服务函数
  - [ ] 核实 `Channel` 到 SSE/WebSocket 的投递契约（阻塞、缓冲、背压、关闭行为）
  - [ ] 结论回填 ADR 0012 与研究报告第 9 节；不成立时改写桥接层方案
  - 验收：两项未知各有明确结论与可复现的最小验证程序；ADR 中的桥接层假设被证实或被替换

- [ ] **实现服务端 HTTP 传输层与静态前端交付**
  - [ ] 单进程同时提供命令 API、图片端点与静态前端产物；图片端点具备 Range 语义（`Accept-Ranges`、单范围 206、越界拒绝）
  - [ ] 图片与文件端点按工作区根目录与项目归属授权，不沿用桌面态的全通配范围
  - [ ] 桌面专属能力（自我更新、进程重启）在服务端形态下不可用，并有明确提示
  - 验收：浏览器可加载大图与视频帧且拖动/seek 正常；越出授权目录的路径请求被拒绝；静态前端不依赖 Tauri 的 CSP 与 asset 协议配置

- [ ] **实现服务端账号与身份**
  - [ ] 用户名 + 口令的本地账号，口令以哈希存储；首个账号在部署初始化时创建，会话凭证可撤销
  - [ ] 所有 API 调用携带身份，未认证请求被拒绝；不做 RBAC 与租户隔离
  - [ ] 身份用于操作归属、取消权与并发冲突提示
  - 验收：未登录无法读写任何项目；两个账号并发操作同一项目时冲突提示能指明对方身份；口令不落明文

- [ ] **实现工作区根目录白名单与目录选择**
  - [ ] 管理员配置可访问的工作区根目录，浏览器端的项目目录选择被限制在其中
  - [ ] 沿用「图片目录即项目 + 同目录 my-label-tool.project.json」模型，不引入上传存储
  - [ ] 所有路径读写校验位于白名单根目录内，拒绝 `..`、符号链接与重解析点逃逸
  - 验收：白名单外路径一律拒绝；共享盘 / NAS 挂载目录可直接作为工作区根目录；桌面形态行为不变

- [ ] **实现项目文档并发控制（ETag / If-Match）**
  - [ ] 项目保存走 HTTP 前置条件，版本冲突返回 412，界面给出可操作提示（重新加载 / 放弃本次保存）
  - [ ] 项目文件格式不变：不加版本字段，不改 ProjectConfig schema 与桌面端行为
  - [ ] 冲突提示按操作归属展示，任何情况下不静默覆盖他人修改
  - 验收：两浏览器并发保存同一项目时后提交者收到明确冲突且磁盘内容未被覆盖；单用户连续保存无额外提示

- [ ] **实现服务端任务注册表与取消权归属**
  - [ ] 长任务（导出、预打标、抽帧、插件调用、保存）登记归属账号、占用资源、进度与取消句柄；取消权按归属判定（发起者或管理员）
  - [ ] 浏览器断线不取消任务，重连后可看到进度与结果；桌面形态保持关窗即取消
  - [ ] 占用同一资源的任务互斥沿用现有操作资源模型
  - 验收：两用户各发一个长任务互不误杀；发起者断网重连后仍能看到并取消自己的任务；非归属账号看不到取消入口

- [ ] **实现服务端预打标作用域与推理队列**
  - [ ] 全局模型库、全局 ONNX 运行时与共享内存预算；多人并发时按队列执行，并发上限可配
  - [ ] 排队位置与预计等待对发起者可见；管理员可调整上限
  - 验收：三人同时发起预打标按队列执行且不超预算；模型只下载一次；队列中的任务可被归属账号取消

- [ ] **落地服务端插件启用策略**
  - [ ] 数据型插件（预置标签）在服务端始终可用；代码型插件仅 Windows 宿主且管理员显式开启时可用，其他宿主禁用并说明原因
  - [ ] 服务端插件安装与授权沿用现有契约与权限模型，不新增插件能力、不修改任何版本面
  - 验收：非 Windows 宿主上代码型插件入口不可用且原因可见；Windows 宿主显式开启后导出/预打标插件可用；插件 manifest、宿主 API 与协议版本保持 v1

- [ ] **服务端交付形态、部署文档与平台替换项**
  - [ ] 提供容器化交付（服务端二进制 + 静态前端），配置覆盖监听地址、工作区根目录、账号初始化与推理并发上限
  - [ ] 明确 npm 形态边界：Node 包装器 + 预编译二进制，不承诺纯 npm 运行
  - [ ] 部署文档写明平台替换项：删图改软删除（保留绝不永久删除的不变量）、CPU 推理、代码型插件不可用、ffmpeg 随附与许可处理
  - 验收：干净局域网机器按文档完成部署并可被两个浏览器同时使用；文档明确两个形态的能力差异

## 🚧 开发中 (In Progress)

- [x] **画布平移与滚轮行为惯例化**
  - [x] 新增空格键按住 + 左键拖拽平移画布，平移时光标切换为抓手样式
    - 任务描述：空格监听必须复用现有快捷键门禁（遮罩栈、可编辑焦点、操作互斥裁决），弹窗打开或输入框聚焦时不得触发平移
  - [x] 鼠标中键从「按下即取消草稿」调整为「拖动平移、单击取消」
    - 任务描述：此项变更手势契约，需同步更新手势解析测试、画布组件测试与 docs/frontend-interaction.md（架构评审状态图中中键/空格平移本为既定意图，属回归）
  - [x] 滚轮缩放不再要求指针位于图片矩形内，全画布区域均可缩放，缩放锚点夹取到图片范围内
  - [x] 默认模式的画布操作提示补充平移说明
  - 验收：放大图片后不按 Ctrl 也能用空格+拖拽与中键拖拽平移；弹窗打开或输入框聚焦时空格不平移；在图片外灰色区域滚动滚轮有缩放反馈；默认模式提示中可见平移说明；手势契约相关测试更新并通过

- [x] **画布缩放指示与视图控件**
  - [x] 画布角落常驻显示当前缩放百分比，初始适应状态显示"适应"字样，缩放后显示百分比
  - [x] 点击缩放指示器恢复适应画布，旁附放大/缩小步进按钮
  - [x] 指示器位置与现有模式提示、标签快捷键浮层的角落互不遮挡，随鼠标位置让位的现有逻辑保持有效
  - 验收：滚轮、键盘、右键菜单三种缩放方式均实时更新百分比；点击指示器恢复适应；1440×900 与更小窗口下不与浮层重叠

- [x] **统一未保存修改的关闭语义**
  - [x] 标签管理弹窗维持现状语义（关闭保留草稿、弹窗内琥珀提示与侧栏 `*`、切模板已有确认），不引入关闭前确认
  - [x] 项目设置中的抽帧间隔修改后直接关闭时提示确认，不再静默丢弃
  - [x] 类名映射行内标签下拉选择后未点"绑定"就切换或关闭时，显示未保存标识或自动应用
  - [x] 各处脏状态标识样式统一（弹窗内琥珀提示与触发按钮回显的 `*`）
  - 验收：抽帧间隔与类名映射修改后关闭均不会出现无提示的静默丢弃；标签弹窗草稿保留行为不回退；各处脏状态标识风格一致

- [x] **草稿标签与标注引用一致性防护**
  - [x] 取消标签修改时检查已存标注引用的标签是否仍存在，存在失效引用时提示并引导处理（移除标注或恢复标签）
  - [x] 仅存在于草稿中的新标签在保存前不可被选为"当前标签（新框使用）"，或选中时明确提示尚未保存
  - [x] 标注渲染遇到缺失标签时给出可感知提示（如警示色 + tooltip），不再无提示套用第一个标签的颜色与名称
  - 验收：用草稿新标签绘制后取消修改，界面出现明确提示且不产生静默改色/改名的标注；处理路径可重复操作

- [x] **错误提示统一与操作归属**
  - [x] 错误消息按操作归属展示：常驻可关闭、不互相覆盖、同操作多次错误聚合
  - [x] 预打标设置内的单一 error 变量与全局 5 秒自动消失的错误条迁移到统一机制
  - [x] 消除同一次失败的重复上报（现状为操作卡片 fail 与全局错误条各报一次）；统一机制沿用操作卡片既有语义——失败常驻、完成自动收起，仅迁移全局 5 秒单槽条
  - [x] 导出失败在导出面板内提供就地错误显示与重试入口（失败已进操作状态卡片，面板内缺就地反馈）
  - [x] 错误容器补充 role=alert / aria-live 语义
  - 验收：连续触发两类操作错误时两条消息同时可见且可分别关闭；同一次失败只呈现一处；屏幕阅读器可感知错误出现；导出失败可在面板内就地重试

- [x] **进度反馈真实性**
  - [x] 不确定总量时进度条使用动画脉冲样式，不再渲染固定 12% 假进度
  - [x] 批量抽帧弹窗增加进度条与逐视频进度（完成/失败/剩余），替代纯文本
  - [x] 侧栏素材进度条统一为"已标注/总数"语义并与相邻文本一致，视频存在时语义不变
  - 验收：无总量的下载显示动画而非固定百分比；批量抽帧可观察逐项推进；侧栏条与文本数值含义一致

- [x] **原生弹窗收敛为应用内组件**
  - [x] 新增通用「确认」与「命名输入」两个应用内遮罩原语，供本任务与后续确认类需求复用
  - [x] 移除界面路径上的 window.alert / window.prompt / window.confirm（现 9 处 alert、2 处 prompt、1 处 confirm）
  - [x] confirmAction（Tauri 原生 confirm，共 11 处）的全部调用点迁移到应用内确认原语，统一确认观感，含 app-utils 等非组件调用点
  - [x] 校验类 alert（快捷键非法/冲突、至少保留一个标签、导入摘要等）迁移为就地错误提示或应用内提示层
  - 验收：全应用交互路径不再出现浏览器原生与系统级对话框；命名与确认场景在应用内完成且可 Esc 取消；原有确认语义与取消分支行为不回退

- [x] **破坏性操作确认分级与危险样式**
  - 任务描述：本任务新增的确认一律使用「原生弹窗收敛为应用内组件」产出的确认原语，该任务先行
  - [x] 按可逆性分级补充确认：开启安全模式、插件停用/清除失败前确认并说明后果
  - [x] .pt 转换保持现有参数确认与命令预览，补充后果分级说明（外部进程执行、预计耗时、失败不影响模型文件），不新增确认弹层
  - [x] 危险操作按钮常态使用危险配色或图标，不再仅 hover 变红，并与主操作按钮保持间距或分区
  - [x] 内置模板未修改时"保存"按钮不再承担"另存为"语义：禁用并常显说明（不再仅 hover title），另存为保留独立按钮
  - 验收：逐项核对高危操作均有与可逆性匹配的确认与后果说明；危险按钮不悬停即可识别；内置模板场景下不存在双语义按钮

- [x] **素材列表缩略图与虚拟化**
  - [x] 缩略图由 Rust 端命令生成并缓存小尺寸缩略图（image 依赖已具备），前端不做原图直载
  - [x] 图片列表行显示懒加载缩略图（视口外不生成/不加载，失败回退文件名）
  - [x] 列表虚拟化渲染，行高方案覆盖视频分组头与帧子列表等异构行
  - [x] 视频帧子列表（独立嵌套滚动容器）同样应用虚拟化
  - [x] 选中定位从 scrollIntoView 调整为按索引滚动，键盘导航、右键菜单行为保持不变
  - 验收：构造 1 万级图片项目，列表滚动流畅、内存不随列表长度线性增长；大帧数视频项目帧子列表同样满足；缩略图生成不阻塞选中与切图

- [x] **作用域与搜索交互改进**
  - [x] Esc 可退出当前作用域（搜索结果/视频帧范围），退出后回到项目序
  - [x] 搜索对话框支持点击背板关闭（结果进入已支持单击预览、再次点击/Enter 确认，维持现状）
  - [x] 右键菜单不再因列表轻微滚动而关闭，菜单项显示对应快捷键提示；滚动关闭策略与列表虚拟化联动设计
  - 验收：纯键盘可完成"打开搜索→进入范围→浏览→Esc 退出"全流程；右键菜单在轻微滚动时保持打开；菜单中可见"删除图片 F8"类提示

- [x] **表单校验反馈**
  - [x] 模型导入/编辑表单逐字段校验：不合格字段显示原因并关联 aria-invalid 与错误描述，按钮禁用原因可感知
  - [x] 设置类表单支持 Enter 提交并明确主提交按钮
  - [x] PT 转换失败状态提供"重试"入口，与现有文案承诺一致，无需返回引导页重来
  - 验收：表单不合格时可定位具体字段与原因；键盘填完表单可 Enter 提交；转换失败后可就地重试

- [x] **可访问性与文案一致性清理**
  - [x] 补齐无障碍缺口：自定义导出 textarea 与新标签输入的 aria-label、进度条 role=progressbar 与 aria-valuenow、ScopeBar 变化播报
  - [x] "暂未实现"菜单项改为 disabled + 原因提示，与同菜单导入项处理一致
  - [x] 插件管理弹窗顶部的权限说明文案移至权限确认弹窗
  - [x] 快捷键设置增加"恢复默认"入口（单动作与全部两档），恢复前确认
  - 验收：自动化可访问性检查无 critical 缺口；菜单中不可用项不可点击且有原因；快捷键可一键恢复默认

## ✅ 已完成 (Completed)

- [x] **接入 Konva 画布展示图片**
  - [x] 安装 `konva` + `react-konva`
  - [x] 用 `Stage` + `Layer` + `Image` 替换上一步的原生 `<img>`，正确显示当前选中图片
  - [x] 处理图片自适应画布容器大小（初始显示时按比例缩放到容器内，不裁切）

- [x] **实现图片文件夹选择与列表加载**
  - [x] Rust 端新增 command：读取指定文件夹下所有图片文件路径（支持 jpg/png/bmp）
  - [x] 前端调用该 command，展示图片文件名列表（先用简单列表，不做美化）
  - [x] 点击列表项能在主区域显示对应图片（先用原生 `<img>` 展示，不接入 Konva）

- [x] **定义标注数据核心类型**
  - [x] 在 `src/types/annotation.ts` 中定义 `AnnotationShape`、`LabelConfig`（先只用到 `rect` 类型，图形字段随后续任务逐步扩展）
  - [x] 明确并在类型文件顶部注释写清楚坐标系约定（归一化坐标 or 原图像素坐标），此约定后续不再更改

- [x] **接入 Tailwind CSS 与 Zustand 状态管理**
  - [x] 安装 Tailwind 及其 PostCSS 依赖，配置 `tailwind.config.js` 的 content 路径覆盖 `src/**/*.{ts,tsx}`
  - [x] 在 `App.tsx` 中写一个测试用的 Tailwind class，确认样式生效后移除
  - [x] 安装 `zustand`，在 `src/store/` 下创建一个空的示例 store（如 `useAppStore.ts`），验证跨组件读写状态可用后可保留或删除

- [x] **接入基础开发工具链（Lint/格式化/静态检查）**
  - [x] 安装并配置 ESLint + TypeScript 规则（禁止 `any`，与项目编码规范对齐）
  - [x] 安装并配置 Prettier，与 ESLint 规则不冲突
  - [x] 在 `package.json` 中添加 `lint`、`typecheck` script
  - [x] 配置 Rust 端 `cargo clippy` 可正常运行（`cargo clippy --manifest-path src-tauri/Cargo.toml`）
  - [x] 验证 `npm run tauri dev` 能正常拉起空白窗口

- [x] **初始化项目骨架与本地 Git 仓库**
  - [x] 在当前目录执行 `git init`（仅本地仓库，不关联远程）
  - [x] 创建 `.gitignore`（覆盖 `node_modules/`、`src-tauri/target/`、`dist/`、`.DS_Store` 等）
  - [x] 用 `npm create tauri-app@latest` 初始化 Tauri + React + TypeScript 模板到当前目录
    - 选择 React + TypeScript 模板，包管理器选 npm
  - [x] 确认生成的目录结构与项目约定基本一致，如有出入手动调整（如新建 `src/store`、`src/hooks`、`src/lib` 等空目录）
  - [x] 执行首次 `git add . && git commit`，确认 `AGENTS.md` 与 `ROADMAP.md` 也被纳入本次提交

- [x] **实现矩形框绘制与选中**
  - [x] 鼠标按下-拖动-松开在画布上绘制一个矩形（用临时状态存储绘制中的矩形，松开后转为正式 `AnnotationShape`）
  - [x] 已绘制的矩形正确渲染在画布上（边框颜色可先用固定色）
  - [x] 点击已有矩形可选中（选中态有视觉区分，如边框加粗或出现控制点）

- [x] **实现矩形框的移动与缩放调整**
  - [x] 选中矩形后可拖拽整体移动
  - [x] 选中矩形后四角/边中点出现可拖拽的控制点，用于调整宽高
  - [x] 调整后的坐标正确写回对应 `AnnotationShape` 数据

- [x] **绑定固定标签到矩形框**
  - [x] 硬编码 2-3 个 `LabelConfig`（如“人”“车”“其他”，先不做配置UI）
  - [x] 新建矩形时默认绑定第一个标签，绘制完成后可通过简单下拉框修改所属标签
  - [x] 不同标签的矩形框用不同颜色区分

- [x] **实现单张图片标注数据的内存管理**
  - [x] 建立 `annotation` store（Zustand），按图片路径为 key 存储各图片的 `AnnotationShape[]`
  - [x] 切换图片时正确保存当前图片标注、加载目标图片已有标注（先全部保存在内存，不落盘）

- [x] **实现标注数据导出为 JSON**
  - [x] Rust 端新增 command：接收前端传来的标注数据结构，写入用户指定路径的 `.json` 文件
  - [x] 前端加一个“导出”按钮，调用该 command，导出当前所有图片的标注数据
  - [x] 手动验证导出的 JSON 内容与画布上标注状态一致（坐标、标签、图片路径对应关系正确）

- [x] **实现标签配置面板 UI**
  - [x] 新建 `components/settings/LabelSettings.tsx`，展示当前所有 `LabelConfig` 列表，替代此前硬编码的标签
  - [x] 支持新增标签（输入名称，自动分配一个默认颜色）
  - [x] 支持删除标签（若该标签已被使用中的矩形框引用，删除前需提示）
  - [x] 支持修改标签颜色（用颜色选择器）

- [x] **标签配置持久化到本地文件**
  - [x] Rust 端新增 command：读取/写入标签配置到本地 JSON 文件（如放在应用数据目录）
  - [x] 应用启动时自动加载上次保存的标签配置
  - [x] 标签配置面板中的增删改操作实时同步保存到本地文件

- [x] **实现标签快捷键绑定**
  - [x] 在标签配置面板中，每个标签可设置一个单键快捷键（如数字键、字母键）
  - [x] 校验新绑定的快捷键是否与其他标签冲突，冲突时提示并阻止保存
  - [x] 按下对应快捷键时，当前选中的矩形框标签切换为该快捷键对应标签

- [x] **预置常用标签模板**
  - [x] 设计 1-2 套预置标签模板（如通用目标检测常见类别），存放在 `src/lib/defaults/`
  - [x] 标签配置面板中提供“加载预置模板”按钮，可一键导入（需处理与现有标签的合并/覆盖逻辑）

- [x] **设计导出模板数据结构与 UI 骨架**
  - [x] 在 `src/types/export.ts` 中定义 `ExportTemplate` 类型
  - [x] 新建导出面板 UI，展示可选导出格式列表（先只列出，不实现具体转换逻辑）

- [x] **实现 COCO 格式导出**
  - [x] 在 `src/lib/exporters/coco.ts` 中实现内部标注数据 → COCO JSON 结构的转换
  - [x] Rust 端或前端完成文件写入（复用此前 JSON 导出流程的 command，调整输入数据结构）
  - [x] 用 `pycocotools`（或在线校验工具）验证导出文件格式合法

- [x] **实现 VOC 格式导出**
  - [x] 在 `src/lib/exporters/voc.ts` 中实现内部标注数据 → VOC XML 结构的转换（每张图一个 XML 文件）
  - [x] 验证导出的 XML 文件字段完整（bndbox、name、size 等）且能被常见 VOC 解析工具读取

- [x] **实现 YOLO 格式导出**
  - [x] 在 `src/lib/exporters/yolo.ts` 中实现内部标注数据 → YOLO txt 格式（归一化中心点+宽高）的转换
  - [x] 同时生成对应的 `classes.txt` 标签索引文件
  - [x] 验证坐标归一化计算正确（抽样人工核对至少3个标注框）

- [x] **实现自定义导出格式（字段映射）**
  - [x] 设计一个简单的字段映射配置 UI（如 JSON 结构模板，用户可指定字段名对应关系）
  - [x] 在 `src/lib/exporters/custom.ts` 中实现基于该映射配置的通用转换逻辑
  - [x] 用一个实际自定义模板跑通一次完整导出流程

- [x] **实现快捷键速切当前标签**
  - [x] 按下标签绑定的快捷键时，若当前有选中的矩形框，直接切换其标签
  - [x] 若当前无选中框，则切换“下一个新建矩形框默认使用”的标签
  - [x] 界面上有明显指示当前默认标签是什么（如工具栏高亮）

- [x] **实现标注导入功能**
  - [x] 明确第一版导入范围：支持本工具原生 JSON、COCO、VOC、YOLO，暂不做 custom 反向导入
  - [x] 导出原生 JSON 标注时，同步保存项目配置文件（建议放在图片文件夹或导出文件同级）
    - [x] 配置中保存 `schemaVersion`、标注文件路径、导出格式、导出时间、图片目录信息
    - [x] 配置中保存当前标签快照（`LabelConfig[]`）、当前模板信息、必要的导出选项
    - [x] 标注数据继续使用现有 `AnnotationExport` 结构，坐标仍为原图像素 `[x, y, width, height]`
  - [x] 打开图片文件夹后，若目录内存在项目配置文件，提示用户自动加载，而不是静默覆盖当前状态
  - [x] 界面上添加“导入标注”按钮
    - [x] 需要已打开图片文件夹才可使用
    - [x] 点击后选择导入格式：原生 JSON / COCO / VOC / YOLO
    - [x] 原生 JSON 导入：选择项目配置文件，校验配置版本、标注文件是否存在、图片目录是否匹配
    - [x] COCO 导入：选择 `.json` 文件，解析 `images`、`annotations`、`categories`，将 `bbox` 转为内部矩形坐标
    - [x] VOC 导入：选择包含 `.xml` 的目录，按 `filename` 匹配当前图片，解析 `bndbox` 为内部矩形坐标
    - [x] YOLO 导入：选择标注目录，读取 `classes.txt` 与同名 `.txt`，将归一化中心点坐标转回原图像素坐标
    - [x] 导入前若当前已有未保存/未导出的标注，需要二次确认，避免覆盖
    - [x] 按当前图片列表匹配标注数据（优先路径匹配，路径不一致时按文件名兜底并提示缺失图片）
  - [x] 为导入配置生成一个项目专用模板，只在本次项目配置下使用
    - [x] 原生 JSON 从配置中的标签快照生成模板；COCO/VOC/YOLO 从导入文件中的类别生成模板
    - [x] 修改标签时写回项目配置，不污染全局标签配置或通用模板
    - [x] 项目专用模板不可删除；若用户切换到其它模板，需要提示可能影响当前项目标注标签
  - [x] 手动验收：原生 JSON 重启后可恢复；COCO/VOC/YOLO 各导入一个样例后，标签、图片匹配、矩形坐标均正确

- [x] **实现画布缩放（滚轮缩放 + Ctrl 微调）**
  - [x] 监听画布 `wheel` 事件，计算缩放前后鼠标指向的图片坐标点保持不变
  - [x] 设置合理的最小/最大缩放级别限制
  - [x] 区分普通滚轮与 Ctrl+滚轮两种缩放步长（Ctrl 时更细粒度），验证两种模式切换流畅、无跳变感
  - [x] 验证缩放后已有标注框仍与图片内容对齐（无偏移）

- [x] **实现快捷键切换图片**
  - [x] 绑定默认快捷键（如 `A`/`D` 或方向键）切换上一张/下一张图片
  - [x] 切图时正确保存当前图片标注状态、加载目标图片标注状态（复用已有的 annotation store 逻辑）
  - [x] 图片列表面板同步高亮当前选中项，并自动滚动到可视区域

- [x] **实现通用快捷键配置面板**
  - [x] 新建 `components/settings/ShortcutSettings.tsx`，列出所有可配置动作（缩放、切图、删除框、撤销/重做等）及当前绑定键
  - [x] 支持点击后进入“录制”状态，按下新键位完成改绑
  - [x] 改绑后的快捷键持久化保存到本地配置文件，应用重启后生效
  - [x] 校验新绑定是否与已有快捷键冲突

- [x] **交互模式扩展（Shift 强制选择 / Ctrl 强制标注）**
  - [x] 引入交互模式状态，通过 Shift/Ctrl 按键实时切换（按下切入、松开回退默认）
  - [x] Shift 持按：强制选择模式（左键仅选择/取消选择不绘制，右键菜单不变，Ctrl+右键平移不变，中键不变；滚轮不缩放改为在重叠候选框之间循环切换高亮目标，点击左键选中当前高亮框）
  - [x] Ctrl 持按：强制标注模式（左键直接绘制、忽略已有框命中检测，右键菜单不变，Ctrl+右键平移不变，滚轮正常缩放，中键不变）
  - [x] 模式切换时取消进行中的绘制状态，避免跨模式残留
  - [x] 重叠区域候选框需有排序逻辑，需选定一种并保持一致
  - [x] 当前状态指示：在画布角落显示当前模式下各输入的操作说明（默认不显示），半透明背景小字号，不遮挡标注区域，按键瞬间实时更新

- [x] **实现撤销/重做栈**
  - [x] 设计基于操作记录（新增/删除/移动/调整标注框）的撤销栈数据结构
  - [x] 绑定默认快捷键（如 `Ctrl+Z` / `Ctrl+Y`）触发撤销/重做
  - [x] 验证连续多步操作后撤销顺序正确，重做后状态与撤销前一致

- [x] **实现删除标注框与批量清空**
  - [x] 选中矩形框后按 `Delete` 键删除，操作计入撤销栈
  - [x] 提供“清空当前图片所有标注”按钮（需二次确认，避免误触）

- [x] **优化画布性能与大图片处理**
  - [x] 测试较大分辨率图片（如 4K 以上）加载与标注时的流畅度
  - [x] 若有明显卡顿，评估是否需要对图片进行降采样展示、标注框坐标仍按原图分辨率存储

- [x] **单文件代码长度限制**
  - [x] 在AGENTS.md中，添加限制
    - 无特殊情况下，单文件代码禁止超过1000行，否则要按功能模块拆分到不同文件
    - 如确实有需要超过1000行的，需要在文件开头以注释写明原因和理由
  - [x] 审查当前所有代码文件，拆分过长的文件使其符合此规范

- [x] **完成 Windows 打包并在无开发环境机器上验证**
  - [x] 执行 `npm run tauri build`，确认生成 Windows 安装包/exe 无报错
  - [x] 在开发机器上运行打包产物，验证功能与 dev 模式一致
  - [x] 找一台未安装 Node/Rust/前端开发环境的 Windows 机器（或全新虚拟机），安装/运行打包产物，记录启动耗时、是否有依赖缺失报错

- [x] **优化打包体积与启动速度（若上一任务发现问题）**
  - [x] 检查 `tauri.conf.json` 中是否有可裁剪的不必要功能模块
  - [x] 检查前端打包产物是否有可优化的冗余依赖（如未使用的图标库、polyfill）
  - [x] 记录优化前后的体积/启动耗时对比

- [x] **编写打包与发布说明文档**
  - [x] 在 `docs/` 下新增 `build.md`，记录打包命令、注意事项、已知问题
  - [x] 记录支持的最低 Windows 版本（如需要）

- [x] **配置 GitHub Actions 自动发布 Windows 安装包**
  - [x] 新增 release workflow：当推送版本 Tag 时触发，匹配 `V0.1.0` / `v0.1.0` 这类格式（大小写不敏感）
  - [x] 从 `changelog.md` 中读取对应版本章节，作为 GitHub Release 的发布说明
  - [x] 执行前端构建与 Tauri 打包，生成 Windows 安装包产物
  - [x] 将安装包上传并发布到对应 GitHub Release

- [x] **从非项目标注目录创建项目**
  - [x] 使用场景：用户手上只有第三方数据集目录（如图片目录 + YOLO 标注目录），没有本工具生成的项目配置文件时，可直接导入并生成一个本工具项目
  - [x] UI 入口放在“导入标注”下的二级菜单中，区分“导入本工具项目”和“从外部格式创建项目”
  - [x] 第一版仅实现 YOLO 格式：复用当前已打开的图片目录，选择标注目录，读取同名 `.txt` 标注文件；若存在 `classes.txt`，按其生成标签，否则用类别序号生成临时标签名（如 `class_0`、`class_1`）
  - [x] COCO / VOC / Custom 入口先保留为不可用项，点击后提示“暂未实现”，不写占位解析逻辑
  - [x] 导入时按当前图片列表匹配标注文件，YOLO 归一化坐标转换为内部原图像素坐标 `[x, y, width, height]`
  - [x] 导入完成后在图片目录下保存项目配置文件，记录图片目录、标注来源、导入格式、标签快照与生成时间，后续再次打开该目录时按本工具项目加载
  - [x] 为本次导入生成项目专用标签模板，写入项目配置，不污染全局标签配置或通用模板
  - [x] 导入前若当前已有标注数据，必须二次确认，避免覆盖；导入后展示缺失图片、缺失标注文件、非法标注行的数量摘要
  - [x] 手动验收：使用一个 YOLO 样例目录导入后，重启应用重新打开图片目录，标注框、标签与项目配置均能恢复

- [x] **优化连续打标时的标签切换体验**
  - [x] 背景反馈：连续画框时，用户按标签快捷键通常是为了设置“下一个框”的默认标签，而不是修改刚画完的框
  - [x] 当前问题：新建矩形后仍保持选中，立即按标签快捷键会把刚完成的矩形改成新标签，容易误改已确认标注
  - [x] 交互规则调整
    - [x] 绘制完成后不再自动选中新矩形，按标签快捷键只切换后续新建矩形的默认标签
    - [x] 用户主动选中已有矩形后，按标签快捷键仍应修改该矩形的标签
    - [x] 删除、撤销/重做、拖拽调整等现有选中框操作不受影响
  - [x] 切换反馈
    - [x] 切换默认标签时，在画布上方显示 1-2 秒轻量提示，包含标签名与颜色
    - [x] 提示不抢焦点、不阻挡继续绘制，也不写入撤销栈
  - [x] 设置与标注展示
    - [x] 将菜单中的“快捷键设置”升级为统一“设置”，集中放置快捷键、标签显示等配置
    - [x] 增加标签名显示配置：默认模式、选择模式、标注模式分别独立开关
    - [x] 按当前交互模式渲染标注框标签名，关闭对应模式后不显示标签名

- [x] **优化大图切换时的加载反馈与预载**
  - [x] 背景反馈：较大图片切换时会短暂停顿，用户难以判断是图片仍在加载还是界面卡住
  - [x] 图片预载
    - [x] 当前图片加载完成后，后台预加载下一张图片
    - [x] 切到已预载图片时优先复用缓存，减少空白等待时间
    - [x] 图片列表、标注数据保存/加载逻辑保持不变
  - [x] 加载反馈
    - [x] 图片未就绪时显示无具体百分比的轻量加载条或加载动画
    - [x] 加载提示只表达“正在处理”，不承诺精确进度
    - [x] 加载失败时保留现有错误提示，不用加载动画吞掉错误

- [x] **优化保存反馈与快捷键**
  - [x] 背景反馈：图片数量变多后保存会短暂停顿，用户难以判断是否正在保存
  - [x] 保存时显示无具体百分比的进度条，至少保留 0.5 秒
  - [x] 即使 0.5 秒内保存完成，也等动画显示满 0.5 秒后再消失
  - [x] 保存成功后显示“保存完成”提示
  - [x] 绑定 `Ctrl+S` 触发当前项目保存，不弹出另存为文件选择

- [x] **优化画布模式操作提示配置**
  - [x] 设置中增加“是否显示模式操作提示”总开关
  - [x] 模式操作提示补充 `Ctrl+S` 保存、模式切换与常用鼠标操作说明
  - [x] 设置中增加“是否显示标签快捷键”开关
  - [x] 开启后在常态与各模式提示中展示标签快捷键，如 `1：标签1`、`q：标签2`

- [x] **增加标注定位辅助**
  - [x] 当前左键操作会新建标注框时，鼠标指针显示为十字精确选择样式
  - [x] 默认模式下指向已有标注框时保持普通选择行为，不显示标注十字光标
  - [x] 标注时显示随鼠标移动的水平/垂直十字辅助线，用于辅助定位
  - [x] 在统一设置中分别提供“标注时使用十字光标”和“显示十字辅助线”开关

- [x] **优化标签快捷键浮层**
  - [x] 右上角标签快捷键列表高亮当前新建标注框会使用的标签
  - [x] 高亮依据默认绘制标签，不跟随当前选中框的标签变化

- [x] **基于 GitHub Release 的自动更新功能**
  - [x] 目标：应用启动后或用户点击“检查更新”时，从 GitHub Release 查询新版本，提示用户下载并安装；第一版不做自建更新服务器、不做强制静默更新
  - [x] 实现要求（AI 助手负责）
    - [x] 接入 Tauri v2 updater 插件：添加 Rust/前端依赖、注册插件、配置 `tauri.conf.json` 的 updater `pubkey` 与 GitHub Release `latest.json` endpoint
    - [x] 复用现有 `.github/workflows/release.yml`，让发布产物包含 updater 需要的安装包、签名与 `latest.json`
    - [x] 新增前端更新入口：启动后轻量检查一次，并在设置/帮助区域提供“检查更新”按钮
    - [x] 更新提示需显示当前版本、新版本、发布说明摘要，并让用户明确选择“立即更新/稍后”
    - [x] 更新下载/安装过程中显示状态反馈；失败时给出可理解错误，不影响继续离线使用当前版本
    - [x] 将自动更新发布流程补充到 `docs/build.md`，包括版本号、tag、changelog、签名密钥与验证步骤
    - [x] 验收：本地构建通过 `npm run typecheck`、`npm run lint`、`cargo clippy --manifest-path src-tauri/Cargo.toml`；用一个高版本测试 Release 验证可发现并安装更新
  - [x] 需要你操作
    - [x] 确认 GitHub Release 是否允许公开访问；若仓库/Release 私有，第一版需改为手动下载或另建更新源
    - [x] 生成 Tauri updater 签名密钥，只把 public key 写入配置；private key 与密码保存到 GitHub Actions Secrets，禁止提交到仓库
    - [x] 每次发布前同步更新 `package.json`、`src-tauri/Cargo.toml`、`src-tauri/tauri.conf.json` 的版本号，并在 `changelog.md` 添加同名版本章节
    - [x] 若要降低 Windows SmartScreen 提示，准备代码签名证书；这不阻塞第一版自动更新，但会影响用户信任体验

- [x] **图片列表搜索定位与快速跳转**
  - [x] 目标：图片数量较多时，用户可以按文件名快速过滤、预览并跳转到目标图片
  - [x] 入口
    - [x] 在图片列表区域增加搜索按钮，图标使用放大镜
    - [x] 绑定 `Ctrl+F` 打开搜索浮窗，并自动聚焦搜索框
    - [x] 当焦点位于输入框、文本域等可编辑控件内时，不拦截原生 `Ctrl+F`
  - [x] 搜索浮窗
    - [x] 默认按文件名做普通文本包含匹配，不匹配完整路径
    - [x] 提供“正则”切换按钮；关闭时按普通文本匹配，开启时按正则表达式匹配
    - [x] 正则表达式无效时显示错误提示，不崩溃、不切换当前图片
    - [x] 支持清空搜索词、关闭浮窗；按 `Esc` 关闭浮窗且不改变当前图片
  - [x] 结果与预览
    - [x] 实时展示匹配结果；无结果时显示空状态
    - [x] 当前图片在搜索结果中保持高亮
    - [x] 单击结果仅选中该候选项，并在结果旁显示小图预览
    - [x] 预览加载失败时显示轻量错误占位，不影响继续选择其它结果
  - [x] 跳转确认
    - [x] 对同一候选项再次点击，或选中候选项后按 `Enter`，才切换到该图片
    - [x] 切换图片必须复用现有切图逻辑，保证当前图片标注先保存、目标图片标注正确加载
    - [x] 切换后关闭搜索浮窗，图片列表同步高亮并滚动到当前图片
  - [x] 验收
    - [x] 普通文本搜索、正则搜索、无结果、非法正则均有明确反馈
    - [x] 只预览不确认时，当前图片与画布内容不变化
    - [x] 确认跳转后，画布、图片列表高亮、当前图片标注三者一致

- [x] **项目标签模板变更时保护并同步已有标注**
  - [x] 目标：修改项目专用标签模板时，避免已有标注被静默改错、丢失或导出为错误类别索引
  - [x] 适用范围
    - [x] 仅当当前模板是项目专用模板（`project-config`）时执行影响检查
    - [x] 通用模板、预置模板、另存为模板不检查已有项目标注，沿用现有保存逻辑
    - [x] 内部标注以 `labelId` 关联标签；YOLO/COCO 等导出类别索引由当前标签顺序计算
  - [x] 保存前影响分析
    - [x] 保存项目前，对比“保存前标签列表”和“编辑后标签列表”
    - [x] 输出旧标签表与新标签表：`索引 / id / 名称 / 是否被标注使用`
    - [x] 标签匹配优先使用 `id`；若标签被重新创建导致 `id` 改变，可用唯一名称兜底匹配
    - [x] 若出现重复名称导致无法唯一匹配，要求用户处理后再继续
    - [x] 识别以下变化：
      - [x] 仅在末尾新增标签：无已有标注影响
      - [x] 在中间新增标签或调整标签顺序：已有标注 `labelId` 不变，但导出类别索引会变化
      - [x] 修改标签名称、颜色、快捷键：已有标注引用不变，但导出的类别名称可能变化
      - [x] 删除未被使用的标签：不删除标注，但导出类别索引可能变化
      - [x] 删除已被使用的标签：必须二次确认是否删除这些标注；未确认则取消保存
      - [x] 重新创建同名标签导致 `id` 改变：需要把旧 `labelId` 批量映射到新 `labelId`
  - [x] 保存交互
    - [x] 项目模板存在影响时，保存前展示影响摘要与确认弹窗
    - [x] 摘要至少包含：新增标签、删除标签、重排标签、重命名标签、需重映射标注数、需删除标注数
    - [x] 保留“保存”按钮：只保存标签模板与项目配置，不静默替换已有标注为第一个标签
    - [x] 新增“保存并更新标注”按钮：按影响分析结果同步已有标注引用，并写回项目配置
  - [x] 标注同步规则
    - [x] 用一次性映射表批量更新 `oldLabelId -> newLabelId`，禁止按顺序连环替换，避免 A/B 互换时被重复替换
    - [x] 对被删除且用户确认删除的标签，删除当前项目内所有引用这些标签的标注
    - [x] 标签重排或中间插入标签时，不修改已有标注的 `labelId`；保存后的导出索引自然按新标签顺序生成
    - [x] 同步范围覆盖当前项目已加载的全部图片标注，不只处理当前打开图片
    - [x] 同步完成后更新项目专用模板、`activeProjectConfig.labels` 与项目配置文件
  - [x] 验收
    - [x] 末尾新增标签不会弹出危险删除确认
    - [x] 删除已使用标签时，用户取消则标签与标注均不变化
    - [x] 删除已使用标签且用户确认后，对应标注被删除，其它标注保持原标签
    - [x] `1,2,3,4` 调整为 `2,1,4,3` 后，已有标注仍引用原标签，YOLO `classes.txt` 与导出类别索引按新顺序输出
    - [x] 旧标签被同名新标签替代时，已有标注一次性映射到新 `labelId`，不会发生重复替换
    - [x] 项目模板变更不会污染全局标签配置和通用标签模板
  - [x] 决策
    - [x] 不允许产生悬空 `labelId`；遇到已使用标签被删除时只能取消或“保存并更新标注”

- [x] **实现多边形标注**
  - [x] 支持连续点击添加多边形顶点，双击或按 Enter 完成绘制
  - [x] 已完成的多边形可选中，顶点可单独拖拽调整
  - [x] 更新 `coco`/`custom` 导出逻辑以支持多边形数据（VOC/YOLO 视格式本身是否支持决定是否跳过）

- [x] **实现关键点标注**
  - [x] 支持在图片上单击放置关键点，绑定标签
  - [x] 关键点可选中并拖拽调整位置
  - [x] 第一版暂不做关键点分组，保持单点 `point` 数据结构

- [x] **实现图形类型工具栏切换与对应快捷键**
  - [x] 工具栏增加图形类型切换按钮（矩形/多边形/关键点）
  - [x] 绑定快捷键快速切换绘制模式

- [x] **扩展标签配置以支持指定图形类型**
  - [x] `LabelConfig.shapeType` 生效：不同标签仅能用于对应图形类型（或允许通用，视产品决策）
  - [x] 标签配置面板 UI 相应调整，创建标签时可选择适用图形类型

- [x] **图片搜索增强**
  - [x] 增强图片搜索弹窗的普通搜索模式；正则模式行为保持不变
  - [x] 普通模式支持布尔查询语法
    - 空格表示 `and`；显式支持 `and` / `or` / `not` / `-`
    - `-foo` 等价于 `not foo`，即排除匹配 `foo` 的图片
    - 支持英文括号 `(` / `)` 调整分组
    - 不额外引入运算符优先级；除括号外按从左到右求值
    - 关键字大小写不敏感，文件名与标签匹配也保持大小写不敏感
  - [x] 普通模式支持限定符查询，格式为 `@name(value)`
    - `@filename(value)`：文件名包含 `value`
    - `@tag(value)`：图片存在名称匹配 `value` 的标注标签；按当前标签模板的标签名称匹配
    - `@class(value)`：图片存在类别 id 为 `value` 的标注标签；对应 YOLO 导出时的类别序号
    - `@size(expr)`：图片文件大小满足表达式；支持 `>1MB`、`<=512KB`、`10KB`（等于）等写法
    - 无限定符的普通词默认按 `@filename(word)` 处理，兼容原有搜索体验
  - [x] 搜索框输入体验跟上复杂语法
    - 对运算符、限定符、括号、字符串值做基础高亮，帮助用户看清表达式结构
    - 输入 `@` 或限定符名称时提示可用限定符：`filename` / `tag` / `class` / `size`
    - 输入 `@tag(...)` 时按当前标签模板补全标签名称
    - 输入 `@class(...)` 时补全类别序号，并显示对应标签名，例如 `0 person`
    - 补全只辅助输入，不自动改写用户已输入的合法表达式
  - [x] 明确解析失败时的交互
    - 查询语法错误时不静默返回空结果，在搜索框附近显示错误提示
    - 普通模式复用正则模式的非法表达式提示位置，避免新增一套提示 UI
    - 用户输入合法表达式时实时刷新搜索结果；表达式不合法时保留上一次合法搜索结果
    - 未知限定符、括号不闭合、`@size` 表达式非法都视为语法错误
    - 空查询显示全部图片，行为与当前一致
  - [x] 大数据量搜索时保持界面流畅
    - 输入过程中不能明显卡住搜索框、图片列表滚动和弹窗关闭
    - 优先复用现有图片列表与标注数据，避免每次输入都重新读取文件或重新解析全部项目配置
    - 若本地同步匹配会卡顿，再引入防抖、分片计算或 Web Worker，不提前上复杂方案
  - [x] 提供最小可用示例与验收用例
    - `111 qewr` => `@filename(111) and @filename(qewr)`
    - `asdf or @tag(123) -eee` => `@filename(asdf) or @tag(123) and not @filename(eee)`
    - `@tag(person) @size(>1MB)`：搜索包含 `person` 标签且大于 1MB 的图片
    - `(@tag(car) or @tag(bus)) -bad`：搜索车辆相关图片并排除文件名包含 `bad` 的图片
  - [x] 实现时优先把解析、补全、匹配做成纯函数，补充 Vitest 覆盖解析、补全、匹配、错误提示四类逻辑

- [x] **实现预打标模型库管理与添加模型导入表**
  - [x] 新增“预打标”设置面板，提供模型库列表（增删、选择当前模型、切换零成本）
  - [x] 添加模型流程：选择 `.onnx` → 解析并展示导入表：格式识别（YOLOv5/v8/v11）、类别数 `nc`、输入尺寸、类名列表（取自 ONNX 元数据，缺失时以 `class_N` 占位且可编辑）
  - [x] 模型配置持久化到全局配置（路径、名称、conf/IoU 阈值、输入尺寸覆盖、类名表），重启后保留
  - [x] 选择 `.pt` 文件时提示不支持，给出可复制的转换命令（`yolo export model=best.pt format=onnx imgsz=640`）；同目录存在转换产物时直接推荐使用
  - 验收：添加模型后导入表展示正确、类名可编辑；重启后模型库与配置完整恢复；`.pt` 场景走通指引流程

- [x] **实现可选 ONNX 运行时加载与模型校验（Rust）**
  - [x] 在 `src-tauri/src/media/` 新增预打标模块，基于 `ort` 动态加载（`load-dynamic`），运行时 DLL 从指定路径加载，不随安装包分发
  - [x] 运行时缺失时返回明确错误状态，前端引导下载或手动放置（app data 目录下约定路径）；放置后即时识别，无需重启
  - [x] 提供按需下载入口：经用户确认后从项目 Release 资源下载 onnxruntime DLL（带校验）；离线用户可手动下载放置
  - [x] 模型加载时校验 ONNX 输入/输出张量形状，识别 v5（多分支）与 v8/v11（单输出）并推断 `nc` 与输入尺寸；不兼容模型（如内置 NMS 导出）给出可理解报错并注明仅支持标准导出
  - 验收：未放 DLL 时错误提示清晰可操作；手动放置后功能可用；v5/v8 样例模型解析正确

- [x] **实现推理管线与后处理（解码→letterbox→推理→NMS→像素坐标）**
  - [x] 图片解码（`image` crate）→ letterbox 缩放（灰色 114 填充、居中）→ HWC→CHW 归一化，预处理与 ultralytics 保持一致
  - [x] 会话复用：批量推理时模型只加载一次，推理循环复用同一会话
  - [x] 后处理按输出形状自适应：v5 多分支 anchors 解码、v8/v11 单输出 sigmoid；conf 过滤 + NMS；letterbox 逆变换还原原图像素坐标 `[x, y, width, height]`
  - [x] 返回值含类别索引与置信度；不引入归一化坐标，遵守 `AnnotationShape.points` 像素坐标契约
  - 验收：Rust 单测覆盖 v5/v8 各一个真实模型样例、NMS 与坐标换算；单图推理结果与 ultralytics 官方推理对齐

- [x] **实现模型类名与项目标签映射（复用当前标签表）**
  - [x] 映射界面：模型类名列表按名称自动匹配当前项目标签（精确匹配 + ASCII 大小写不敏感兜底）；未匹配项提供三选一：绑定已有标签 / 从类名新建标签 / 排除该类（不生成标注）
  - [x] 映射关系按模型维度存入 `ProjectConfig`（新增可选字段，旧项目配置加载不受影响，遵守配置兼容性约定）
  - [x] 预打标生成标注时按映射绑定 `labelId`；允许带未匹配项运行（该类跳过），界面醒目提示未匹配数量
  - 验收：名称一致时零配置直接匹配；未匹配项三种处理均正确；旧项目配置升级后行为不变

- [x] **实现预打标执行（单图 + 批量）与结果合并**
  - [x] 单图预打标按钮与整目录批量预打标（进度反馈、可中断，中断后已完成图片的标注保留）
  - [x] 批量时默认跳过已有标注的图片，提供“强制覆盖”选项；单图预打标总是追加
  - [x] store 新增批量插入 action：每张图一个撤销事务；`attributes.confidence` 记录置信度
  - [x] 结果直接合并为正式矩形标注，复用现有渲染/编辑/导出链路
  - 验收：单图/批量流程跑通；撤销逐图回退；手动验证清单通过（预打标框缩放后对齐、切图后标注保存、导出可被标准工具解析）

- [x] **实现 .pt 一键转换（v2，检测到 Python 环境时）**
  - [x] 检测本机 Python/ultralytics 环境（`yolo` 命令或 import 探测），可用时提供应用内一键转换入口
  - [x] 转换产出 `.onnx` 后引导直接加入模型库；未检测到环境时回退为 v1 指引
  - 验收：已装与未装 Python 环境的两台机器各自走通对应路径

- [x] **增强 .pt 自动转换：uv 兜底、参数弹窗与实时输出/中止**
  - [x] 转换环境检测顺序调整为：PATH 中有 `yolo` 命令 → PATH 中有已安装 yolo 相关模块的 Python → PATH 中有 `uv`
    - 任务描述：`yolo` 与已装 ultralytics 的 Python（`python`/`python3`/`py` 任一可用即可）直接执行相应命令；仅当两者都不可用时回退到 `uv`，经 `uvx` 执行转换；`uv` 探测只验证存在性，不触发任何安装
  - [x] uv 环境下经 `uvx` 执行转换，命令显式指定包名
    - 任务描述：`yolo` 命令由 `ultralytics` 包提供，必须使用 `uvx --from ultralytics yolo ...` 形式，禁止 `uvx yolo`（会尝试安装名为 yolo 的包）；首次执行联网安装依赖，命中 uv 缓存后不再联网；转换界面须提示首次转换将联网下载依赖
  - [x] 发起转换时弹出参数确认弹窗
    - 任务描述：弹窗展示可配置参数（`imgsz` 默认 640、须为 32 的倍数；`simplify` 默认关闭）与将要执行的完整命令，用户确认或修改后执行；不持久化用户输入
  - [x] 转换过程实时显示命令行输出
    - 任务描述：输出逐行实时展示（含 uvx 首装的下载/安装进度）；命令行进度条类 `\r` 输出归一化为换行展示；uvx 模式放宽 `PIP_NO_INDEX`/`HF_HUB_OFFLINE` 以允许联网安装，另两种模式维持全量离线环境变量
  - [x] 转换期间用户可随时中止
    - 任务描述：中止需终止整个进程树（Windows `taskkill /T /F`，Unix 进程组），清理临时目录并释放目标路径占用；不产出 .onnx；中止后回到转换引导并提示已中止；转换期间设置弹窗保持锁定；应用退出时终止仍在运行的转换进程；超时护栏：yolo/Python 模式 10 分钟、uvx 模式 20 分钟
  - 验收：
    - 三台分别仅有 yolo 命令、仅有已装 ultralytics 的 Python、仅有 uv 的机器，各自走通 .pt → .onnx 转换并正确加入模型库
    - uv 机器首次转换展示依赖安装输出且成功；断网时安装失败信息可见可理解；再次转换不再安装
    - 转换中输出实时滚动展示；点中止后进程树无残留、无 .onnx 产物，且可立即重试
    - 参数弹窗修改 `imgsz`/`simplify` 后，产物元数据与命令预览一致（如改为 1280 后模型输入尺寸为 1280）

- [x] **预打标运行时下载/推理可取消，支持可选 DirectML GPU 加速**
  - [x] 目标：ONNX Runtime 下载与预打标批量推理支持中途取消；下载在断网/弱网下定时超时而非无限挂起；模型推理设备可配「自动 / 仅 CPU / 仅 GPU」，自动优先 DirectML 失败回退 CPU；下载与推理的模型/网络编排逻辑沉入 `src-tauri/src/media/prelabel/`，command 只做参数校验与结果转发
  - [x] 下载取消/超时：`send().await` 与响应头读取纳入取消/超时 `select!`，连接建立后响应头迟迟不返回不再无限等待
  - [x] DirectML 回退：auto 模式在 DirectML 注册成功但创建 Session 失败时重试 CPU（保证回退 CPU）
  - [x] 取消完成竞态：任务完成并已从注册表移除后点击取消返回「已完成」而非「任务不存在」，前端不再误显推理失败
  - [x] 关键测试：下载响应头超时与取消、流式下载中断、DirectML 失败回退 CPU、取消与正常完成竞态、DirectML 推理路径
  - 验收：`cargo clippy --manifest-path src-tauri/Cargo.toml` 通过、Rust 单测全绿；前端 `npm run typecheck && npm run lint && npm test` 通过；提交同步 `src-tauri/Cargo.lock`，不修改 `changelog.md`，并更新 `AGENTS.md` 命令清单

- [x] **插件系统契约：Manifest Schema、稳定 ID 与版本分层**
  - [x] 目标：定义插件系统全部对外契约（manifest 结构、校验语义、版本规则），作为后续所有插件任务的实现依据；产出 `src/types/plugin.ts`（前端契约）+ Rust 端 `src-tauri/src/plugins/manifest.rs`（字段一一对应）+ `docs/plugin-manifest.schema.json`（JSON Schema）+ 校验单测
  - [x] `PluginManifest` 字段（除注明外均必填）：
    - `schemaVersion`：manifest 结构版本，当前恒为 1；解析时未知版本返回 `UNSUPPORTED_VERSION`
    - `id`：反向域名命名空间，正则 `^[a-z0-9]+(\.[a-z0-9]+){1,}$`（如 `dev.acme.xxx`），永久稳定不复用；格式非法直接校验失败
    - `name`：显示名称，可变更
    - `version`：插件自身版本，semver 格式
    - `apiVersion`：`{ min }`——插件编写所针对的宿主 API 目标版本（必填，见 ADR 0007）；同一版本号内宿主承诺增量兼容，破坏性变更必须开新版本号；不匹配 → 协商阶段返回 `API_VERSION_UNSUPPORTED`
    - `extensionKind`：`label-preset`（数据型）| `exporter` | `prelabel`（代码型）
    - `runtime`：v1 仅接受 `"process"`；`"wasm"` 等未来值校验期显式拒绝并提示原因，不静默接受
    - `entry`：代码型必填、数据型禁止——`{ command, args? }`，command 为包内相对路径，禁止绝对路径与 `..` 穿越；args 缺省为空数组
    - `capabilities`：可选，缺省为空（能力缺省即不支持）——`{ annotationTypes?: ("rect"|"polygon"|"point")[], batch?: boolean, progress?: boolean, cancel?: boolean, configMigration?: boolean }`；`prelabel` 必须声明非空 `annotationTypes`；业务能力可声明独立目标版本：`capabilities.exporter.apiVersion / capabilities.prelabel.apiVersion`（`{ min }`，缺省 = 整体 `apiVersion`），部分能力的破坏性升级只影响声明该能力的插件
    - `permissions`：可选，缺省为空（默认全拒绝）——只接受 `fs.read:<目录>` / `fs.write:<目录>`（目录支持 `%PROJECT%`/`%MODELS%`/`%APP_DATA%` 占位符）与 `network`；重复声明或未知权限面校验失败
    - `configVersion`：可选整数，缺省 0
  - [x] `parsePluginManifest(input: unknown)` 校验语义：不抛异常；失败返回 `{ ok: false, errors: [{ field, code, reason }] }` 收集全部错误（字段路径 + 错误码 + 中文原因）；未知顶层字段忽略（前向兼容）；可选字段按默认值归一化；成功返回带默认值的完整对象
  - [x] 版本分层约定（写入类型注释与契约文档）：插件版本（`version`，随插件迭代）/ 宿主 API 版本（整体版本 + `exporter`/`prelabel` 业务能力独立版本，宿主按插件目标版本分派实现，破坏性变更开新版本号、旧版本按弃用期保留，见 ADR 0007）/ 协议版本（不进 manifest，随握手 `hello` 消息协商，见协议任务）
  - [x] host API 弃用策略文档（`docs/plugin-api-versioning.md`）：标记 deprecated → 提供替代 → 兼容期（≥2 个 host API 版本）→ 移除；新增字段必须可选且有默认值；禁止删除字段、改变既有语义、重排必需字段
  - 验收：`npm run typecheck` 通过；Vitest 覆盖三类样例——合法（含可选字段缺省归一化）、非法（缺必需字段/坏 id/数据型带 entry/代码型缺 entry/prelabel 缺 annotationTypes/权限重复/apiVersion 缺失或非法/能力级版本非法/schema 未知版本，断言 errors 含字段路径与错误码）、旧版本前向兼容（未知字段被忽略）；`docs/plugin-manifest.schema.json` 与 TS 类型字段名集合一致（单测对比）

- [x] **插件安装与注册流程（发现→静态校验→兼容性协商→授权→注册→启用）**
  - [x] 目标：用户从 zip 文件安装插件直至注册启用，贯穿注册状态机；Rust 端 `src-tauri/src/plugins/registry.rs`（状态机 + 注册表持久化）、`manifest.rs`（复用契约任务校验器）+ Tauri commands + 前端管理 UI
  - [x] `install_plugin(path: String)` 命令：任意后缀 zip 包 → 解压到 app data `plugins/<id>/`（zip crate；解压总大小上限 200MB、单文件 50MB、压缩比防护；条目路径规范化后必须以解压根为前缀防 `../` 穿越；拒绝符号链接条目）→ 读取包根 `manifest.json`（缺失即失败）→ 静态校验 → 同 id 已存在时按「更新」处理
  - [x] 兼容性协商：插件声明的目标版本（整体 `apiVersion.min` + 各业务能力 `capabilities.<name>.apiVersion.min`）vs 宿主支持的版本集合（当前实现 + 弃用期保留实现）；不匹配返回 `API_VERSION_UNSUPPORTED` 且给出可操作原因（如「需要 exporter v1，宿主已移除，请升级插件」）；runtime 可用性探测——entry 为 `python`/`python3`/`py` 或独立 exe，复用 `.pt` 转换的环境检测逻辑（`src-tauri/src/media/pt_conversion.rs` 抽象出共享探测函数）；探测失败返回可操作原因（如「未检测到 Python 3」）
  - [x] 授权：协商通过后向前端返回「权限清单 + 未验证作者警告」；`authorize_plugin(installToken, grants)` 用户逐项确认后才落盘注册；grant 与 manifest 声明不一致（多授/少授/未声明权限）校验失败；权限变更重新走确认流程
  - [x] 注册表持久化到 app data `plugin-registry.json`，每条含 `{ id, name, version, extensionKind, entry, capabilities, grants: [{permission, target}], state, failureCount, lastError, installedAt, updatedAt }`；`state ∈ enabled | disabled | auto-disabled | pending-migration`；注册表加载失败不阻断应用启动（记日志 + UI 提示异常）
  - [x] 状态机：`discovered → validated → negotiated → authorized → registered → enabled`；任一步失败回到未注册并清理解压目录；`disabled`/`auto-disabled` 状态不加载进程
  - [x] Tauri commands（全部经 `lib/tauri-api.ts` 封装，组件禁止直接 `invoke`）：`install_plugin` / `authorize_plugin` / `uninstall_plugin` / `list_plugins` / `set_plugin_enabled` / `get_plugin_status` / `clear_plugin_failures`；错误信息走 Rust i18n（`zh_cn.rs`）
  - [x] 管理 UI `src/components/settings/PluginSettings.tsx`：插件列表（名称/版本/扩展类型/状态徽标）、启用/禁用开关（`auto-disabled` 展示 `lastError` 与失败次数）、卸载（二次确认）、日志查看入口、安装按钮（文件选择 + 授权弹窗）；文案进 `src/i18n/plugin.zh-CN.ts`
  - [x] 更新语义：同 id 重装 = 更新，保留 `grants` 与项目内插件配置；`configVersion` 变化触发迁移流程（见配置任务）；卸载只删包目录与注册表项，项目内插件配置与标签快照保留
  - 验收：正常安装（Python 示例）→ 授权 → 启用；取消授权不残留目录；损坏 zip / 缺 manifest / 非法 manifest / apiVersion 不兼容 / runtime 不可用五类失败各有可操作错误且可重复；卸载后项目文件与标注不受影响

- [x] **插件协议 v1：NDJSON 信封、能力协商、进度/取消/标准错误码**
  - [x] 目标：宿主与插件进程的跨语言通信协议；纯逻辑实现 `src-tauri/src/plugins/protocol.rs`（解析/序列化/错误码，不依赖进程运行时，可独立单测）
  - [x] 传输：stdio，每行一个 JSON 对象（NDJSON），UTF-8；行长度上限 16 MiB（超限返回 `PROTOCOL_ERROR` 并丢弃该行继续读取）
  - [x] 信封：`{ "v": 1, "id": string|null, "type": "request"|"response"|"event"|"control", ... }`；`id` 为请求方递增标识，响应/错误/进度事件回显；协议版本 `v` 独立于宿主 API 版本，解析层强制校验存在
  - [x] 消息形状：`request { method, params }`；`response { result }` 或 `{ error: { code, message, data? } }`（error 与 result 互斥）；`event { event: "progress"|"log", payload }`——progress 的 `percent?: number` 可选（缺省 = 无百分比进度），`id` 关联所属调用；`control { action: "cancel"|"heartbeat", id? }`——cancel 的 `id` 指向被取消请求，插件收到后停止当前调用并以 `CANCELLED` 响应结束，heartbeat 双向探活
  - [x] 标准错误码常量表（与 CONTEXT.md 逐项一致，共 10 个）：`PARSE_ERROR` / `PROTOCOL_ERROR` / `METHOD_NOT_FOUND` / `PERMISSION_DENIED` / `TIMEOUT` / `CANCELLED` / `INTERNAL_ERROR` / `CONFIG_MIGRATION_REQUIRED` / `INVALID_ARGUMENT` / `API_VERSION_UNSUPPORTED`（协商阶段版本不匹配）；解析层只产生 `PARSE_ERROR`（非法 JSON）与 `PROTOCOL_ERROR`（非对象信封/缺 v/未知 type/未知消息类型），其余错误码由分发层与运行时产生
  - [x] 握手 `hello`：宿主 spawn 后首条消息 `request method:"hello"`，params `{ protocolVersion: 1, hostApiVersion: <当前>, supportedVersions: { hostApi: [<整体版本>], exporter?: [..], prelabel?: [..] } }`；插件以自身目标版本校验，不匹配返回 `API_VERSION_UNSUPPORTED`；插件响应实际能力声明（缺省即不支持）；协商结果缓存到会话，后续调用以协商结果为准，禁止按版本号推断能力
  - [x] conformance 测试：纯 Rust 单测——四类消息解析/序列化往返、非法 JSON→PARSE_ERROR、超长行/未知 type/缺 v→PROTOCOL_ERROR、progress 带/不带 percent、cancel 关联 id、10 个错误码逐项存在（含 API_VERSION_UNSUPPORTED）；配套协议文档 `docs/plugin-protocol.md`（信封/消息形状/错误码表/握手/进度/取消）与实现一致
  - 验收：`cargo test` conformance 全绿；文档与实现逐项核对无出入

- [x] **插件运行时与故障隔离（Rust）**
  - [x] 目标：插件进程生命周期管理与隔离保证；`src-tauri/src/plugins/runtime.rs`；导出/预打标/迁移等所有插件能力入口统一经 runtime 调用
  - [x] 进程生命周期：懒启动（首次调用才 spawn，会话内复用，进程退出后下次调用重启）；spawn 用 `std::process::Command`，工作目录 = 插件包根目录，环境注入 `MY_LABEL_TOOL_PLUGIN_DIR`；stdin 写 NDJSON、stdout 按行解析、stderr 逐行收集进环形日志（每插件保留最近 500 行）
  - [x] 调用级超时：默认 30 秒；manifest 可声明 `timeoutMs?` 可选字段（缺省 30000，上限 300000）；超时 = 杀进程树（Windows `taskkill /T /F`，Unix 进程组，复用 `.pt` 转换的进程树终止实现）→ 调用返回 `TIMEOUT` → 插件计入失败
  - [x] 失败计数与自动禁用：连续失败 3 次（超时/崩溃/握手失败计数，成功调用清零）→ 状态 `auto-disabled`，`failureCount` + `lastError` 写入注册表并 UI 展示；`clear_plugin_failures`（手动重新启用）清零
  - [x] 关键路径不阻塞：宿主启动、项目打开、项目保存不 spawn、不等任何插件；未就绪插件的能力入口置灰并显示原因
  - [x] 安全模式：全局开关（设置面板 + 持久化），开启后 runtime 拒绝加载全部代码型插件（`exporter`/`prelabel`），数据型（`label-preset`）不受影响；安全模式期间安装代码型插件被拒绝并提示先退出
  - [x] 应用退出清理：退出时终止所有存活插件进程（复用转换任务的应用退出清理模式）
  - 验收：三个坏插件样例（启动即崩 / 调用死循环 / stdout 写垃圾）分别验证——主程序启动、项目打开、项目保存不受影响；死循环样例 30s 返回 `TIMEOUT` 且进程无残留；连续 3 次后 `auto-disabled` 且可手动恢复；安全模式开启后代码型入口全部置灰

- [x] **插件权限模型落地**
  - [x] 目标：权限声明 → 安装授权 → 运行时强制三层闭环；`src-tauri/src/plugins/permissions.rs`，权限判定为纯函数，单测覆盖
  - [x] 权限面：`fs.read:<目录>` / `fs.write:<目录>`（目录级授予；`%PROJECT%` 当前项目目录、`%MODELS%` 预打标模型目录、`%APP_DATA%` 应用数据目录，授权落盘时解析为绝对路径，禁止解析结果逃离占位符语义）、`network`（布尔）；manifest 声明 `spawn` 直接校验失败（不开放）
  - [x] 默认全拒绝：未授权权限面的任何访问返回 `PERMISSION_DENIED`
  - [x] 宿主代理：插件所需数据（图片路径、标注数据、导出数据）一律由宿主经协议传入；插件请求文件读写走协议方法 `fs.read` / `fs.write`（宿主按授权目录校验，路径规范化后必须落在授权目录内，越权返回 `PERMISSION_DENIED`；协议方法未实现返回 `METHOD_NOT_FOUND`）
  - [x] 网络：v1 一律拒绝——spawn 时清理代理环境变量 + 不实现任何网络协议方法；配置迁移调用期间同样强制断网
  - [x] 授权弹窗：安装时逐项列出权限（含占位符解析后的实际路径预览），用户逐项勾选确认；禁止授予 manifest 未声明的权限
  - 验收：权限判定纯函数单测（未授权路径、授权目录内/外、`..` 穿越、占位符解析、重复声明）；集成验证——插件越权读项目外文件返回 `PERMISSION_DENIED`，协议响应不包含未授权文件内容

- [x] **插件配置存储与迁移**
  - [x] 目标：项目文件保存插件状态，版本落后时经协议迁移；`src-tauri/src/plugins/config.rs`（迁移编排）+ `src/types/plugin.ts` 扩展 ProjectConfig 类型
  - [x] `ProjectConfig` 新增可选字段 `pluginConfigs?: { pluginId, configVersion, config: unknown }[]`——`config` 对宿主不透明，宿主不解释内容；旧项目配置加载不受影响（可选字段 + 加载时过滤未知插件 id）
  - [x] 保存/加载：项目保存时把内存中的插件配置写入 ProjectConfig；打开项目按插件 id 匹配注册表，已卸载插件的配置保留在项目文件中但不可用（不删除，防重装丢失）
  - [x] 迁移流程：打开项目发现某插件 `configVersion` < 插件当前声明 → 调用 `config.migrate`（params `{ fromVersion, toVersion, config }`）→ 插件返回新配置 → 写回内存（不自动写盘，随下次项目保存落盘）；链式迁移逐级调用直到目标版本
  - [x] 迁移失败：超时/崩溃/返回非法结构 → 配置标记「待迁移」（`pending-migration`）、该插件能力降级（导出/预打标入口置灰显示「配置待迁移」）、项目照常打开；用户可重试
  - [x] 插件未声明 `configMigration` 能力时，宿主发现版本落后直接进入 `pending-migration`，不发起调用
  - 验收：三条路径——迁移成功（版本与内容正确写回并随项目保存）、迁移失败（项目正常打开、入口置灰、重试成功）、跨多版本链式迁移（v1→v2→v3 逐级调用且参数正确）；无 `pluginConfigs` 的旧项目加载行为不变

- [x] **插件 SDK 与开发者工具**
  - [x] 目标：让第三方开发者能独立产出合规插件；文档 + 示例 + 校验工具 + conformance 环境
  - [x] `docs/plugins.md`：用户侧（安装、授权、安全模式、故障排查）+ 开发者侧（manifest 编写、协议实现要点、错误码、打包、调试——stderr 日志查看与打印式排查）
  - [x] 示例插件两个：`examples/plugins/label-preset-demo`（纯数据：manifest + labels.json）与 `examples/plugins/prelabel-demo`（Python：manifest + main.py，实现 hello/预打标/进度/取消，仅用标准库）
  - [x] 校验器 CLI：`src-tauri/src/bin/plugin-validator.rs`（cargo 内置 bin）——输入插件 zip 或目录，输出 manifest 校验结果与权限清单预览，退出码区分通过/失败；离线可用
  - [x] 打包脚本 `scripts/package-plugin.mjs`（Node）：把 manifest + 资源打成 zip（后缀可指定），默认白名单 `manifest.json` + `plugin/` 目录，多余顶层文件报错，体积超限报错
  - [x] conformance 测试环境：`src-tauri/tests/plugin_conformance.rs` 集成测试——用测试桩进程（同仓库 Rust bin）跑完协议全部用例（消息格式、错误码、握手、进度、取消、16MiB 上限），桩进程代码即「独立 exe 示例」参考实现
  - [x] `docs/plugin-manifest.schema.json` 与 `docs/plugin-protocol.md` 由前序任务产出，本任务校验其与示例/工具一致
  - 验收：按 `docs/plugins.md` 从零做出示例插件，校验器 CLI 通过、打包脚本产出 zip、本地 conformance 自测通过

- [x] **实现预置标签插件支持（数据型，扩展类型 label-preset）**
  - [x] 目标：把标签模板作为纯数据插件接入现有模板体系（安全模式保留数据型插件的依据）
  - [x] 数据文件：包根 `labels.json`，结构复用 `LabelTemplate`（`{ id, name, labels: LabelConfig[] }`）；缺失/解析失败 = 安装校验失败；manifest 校验 `label-preset` 禁止 `entry`/`runtime`
  - [x] 前端接入：安装启用后出现在「加载预置模板」列表，来源标注「插件：<name>」；点击加载复用现有模板加载逻辑（合并/覆盖语义不变）
  - [x] 生命周期：模板快照进项目配置（现有 template 机制）——插件卸载/更新/禁用不影响已加载模板的项目标注；更新后模板列表用新数据，已加载项目不受影响
  - [x] 文案进 i18n
  - 验收：安装 → 加载模板 → 画布可用新标签 → 卸载插件 → 项目标注与导出不受影响；重启后插件模板列表恢复

- [x] **实现导出格式插件支持（代码型，扩展类型 exporter）**
  - [x] 目标：把导出格式扩展为外部进程插件；复用协议/运行时/权限层，宿主负责写盘
  - [x] manifest 扩展：`exporterOptions?: { formats: { id, displayName, extensions: string[], multiFile: boolean }[] }`（v1 固定走 manifest 声明，不做动态格式声明）
  - [x] 协议方法 `exporter.export`：params `{ formatId, exportData: AnnotationExport, options: Record<string, unknown>, outputBaseName }`（AnnotationExport 为现有内部导出结构；图片路径以相对项目路径传）；响应 `{ files: [{ relativePath, contentUtf8? | contentBase64? }] }`（单文件上限 50MB）；宿主校验 relativePath 安全（无绝对路径/`..`/空）后写盘到用户选择目录
  - [x] 导出面板：格式列表加入已启用插件格式（分组「插件格式」并标注插件名）；插件失败/超时/禁用/`pending-migration` 时入口置灰并显示原因；导出完成提示与现有导出一致
  - [x] 进度/取消：插件声明 `progress` 时进度事件驱动现有进度条；声明 `cancel` 时导出中可取消（control cancel + 宽限期杀进程，返回 `CANCELLED`）
  - [x] 内置 COCO/VOC/YOLO/custom 格式不变，插件格式只增不减
  - 验收：示例导出插件（LabelMe JSON，Python 实现）走通「选图目录 → 选格式 → 导出」完整流程且产物可被外部工具解析；插件返回 `../evil.txt` 等越权路径被宿主拒绝且不写盘；导出中取消无残留进程

- [x] **实现外部预打标程序插件支持（代码型，扩展类型 prelabel）**
  - [x] 目标：第三方预打标程序以插件形式接入现有预打标链路，与内置 ONNX 管线并存
  - [x] manifest：`prelabel` 必须声明非空 `annotationTypes`；`batch`/`progress`/`cancel` 可选声明
  - [x] 协议方法 `prelabel.run`：params `{ imagePaths: string[]（%PROJECT% 内相对路径）, classMappings: [{ modelClass, labelId, labelName }], params: Record<string, unknown> }`；响应 `{ shapes: AnnotationShape[] }`——`points` 必须为原图像素坐标（rect `[x,y,width,height]`、polygon 顶点序列、point `[x,y]`），`labelId` 必须存在于映射结果，`attributes.confidence?` 可选；宿主校验坐标合法后合并
  - [x] 批量与进度：声明 `batch` 时可一次传多图，否则逐图调用；声明 `progress` 时进度事件驱动现有批量进度条（复用无百分比进度语义）；声明 `cancel` 时批量可中断，中断后已完成图片的标注保留（与内置管线语义一致）
  - [x] 复用现有预打标链路：预打标执行层抽象「预打标来源」接口（内置 ONNX 管线与插件实现同一接口）；结果合并、每图一个撤销事务、`attributes.confidence` 记录、跳过已有标注/强制覆盖选项全部复用现有实现
  - [x] 入口：预打标面板「模型来源」选择（内置模型 / 插件名）；插件来源的类别映射 UI 复用现有映射面板
  - [x] 图片访问：插件经权限模型读图（`fs.read:<%PROJECT%>` 授权），宿主不传图片字节
  - 验收：示例预打标插件（Python，实现 hello/run/进度/取消）跑通单图与批量；批量中取消后已完成图片标注保留且进程无残留；非法坐标/未知 labelId 返回 `INVALID_ARGUMENT` 且不产生脏数据；与内置 ONNX 管线切换无状态串扰

- [x] **插件系统整体验收与文档**
  - [x] 端到端验收清单（全部通过才算完成）：坏插件隔离（启动即崩/死循环/垃圾输出）、权限拒绝（越权读、未授权网络）、超时与取消（30s 超时、control 取消）、自动禁用（3 次后 + 手动恢复）、安全模式（代码型全禁、数据型保留）、迁移失败降级（项目正常打开、重试成功）、卸载清理（目录/注册表清理、项目不受影响）、更新流程（保留授权与配置、configVersion 变化触发迁移）
  - [x] 手动验证清单：启用/禁用插件后导出面板与预打标入口状态正确；重启后注册表、授权、插件配置完整恢复；安全模式开关即时生效
  - [x] 文档核对：`docs/plugins.md`、`docs/plugin-protocol.md`、`docs/plugin-manifest.schema.json` 与实现一致；CONTEXT.md 术语与 ADR 无冲突
  - [x] 自动化：`npm run typecheck`、`npm run lint`、`npm run test:coverage`（插件纯逻辑保持 90% 行覆盖）、`cargo clippy`、`cargo test`（含 conformance 与权限单测）全部通过
  - 验收：坏插件/权限/超时/取消/自动禁用/安全模式/迁移/卸载八类场景各有一份可复现的记录（截图或日志）

- [x] **实现图片删除功能（列表右键 / 画布右键 / 快捷键，含防呆确认弹窗）**
  - [x] 图片列表右键删除该文件：在列表文件条目上右键显示「删除图片」入口，删除的是磁盘上的源文件（移入系统回收站），而非仅从列表移除；删除非当前图片时，当前图片与画布保持不变
  - [x] 画布右键删除当前图片：现有画布右键菜单的「图片操作」组中增加「删除当前图片」入口
  - [x] 快捷键删除当前图片：新增可配置动作并纳入快捷键设置面板（支持改绑、冲突校验）；默认键位避开固定键 `Delete`（已用于删除选中标注框）
  - [x] 三个入口共用同一确认弹窗与同一删除逻辑；弹窗打开期间删除快捷键不重复触发，画布快捷键不生效
  - [x] 删除确认弹窗（防呆）
    - [x] 展示待删除文件名；该图片已有标注时提示标注将一并删除及数量
    - [x] 确认按钮在 3 秒倒计时结束后才可点击，倒计时期间禁用并显示剩余时间
    - [x] 禁止使用快捷键确认（Enter、Space 等一律无效），仅允许取消（Esc / 取消按钮）
    - [x] 确认按钮使用红色警告样式，文案明确删除后果（移入回收站、标注一并删除）
  - [x] 删除后的状态切换
    - [x] 删除后图片列表立即移除该文件；当前图片被删时自动切换到下一张，删除的是最后一张时切换到新的最后一张
    - [x] 列表被删空时进入空状态（画布清空、无当前图片）
    - [x] 被删图片的标注数据一并移除，后续保存 / 导出结果不再包含该图片
    - [x] 删除不进入撤销栈；撤销 / 重做不得恢复已删除图片的标注
  - [x] 删除失败（文件被占用 / 权限不足 / 不支持回收站）时给出可理解错误，列表、当前图片与标注状态不被破坏
  - 决策：删除即移入系统回收站（可恢复），不提供永久删除入口
  - 验收：
    - 三个入口行为一致：均弹出同一防呆确认弹窗，确认后文件被移入系统回收站且可从回收站恢复
    - 倒计时 3 秒内确认按钮不可点击；任意快捷键无法确认；取消后文件、列表与标注零变化
    - 删除当前图片后按规则切换（中间位置 → 下一张、最后一张 → 新的最后一张、删空 → 空状态）
    - 被删图片的标注随删除一并移除，导出结果不再包含该图片，撤销 / 重做不会使其标注复活
    - 删除失败时有可理解错误提示，应用状态不损坏
  - 验收记录：[图片删除验收](docs/verification/image-deletion.md)

- [x] **实现视频文件导入与抽帧**
  - [x] Rust 端接入视频处理能力（ffmpeg sidecar 或对应 crate），实现按固定间隔抽帧
  - [x] 前端展示抽帧后的帧列表（类比此前的图片列表加载逻辑）

- [x] **实现视频时间轴 UI**
  - [x] 新建时间轴组件，展示当前视频总帧数、当前帧位置
  - [x] 支持拖动时间轴快速跳转到指定帧

- [x] **复用图片标注画布逻辑标注视频单帧**
  - [x] 确认 `AnnotationShape.frameIndex` 字段正确关联每一帧的标注数据
  - [x] 验证切换帧时标注加载/保存逻辑与图片标注一致

- [x] **实现跨帧标注插值（可选，视需求优先级）**
  - [x] 设计关键帧标注 + 中间帧线性插值的数据结构
  - [x] 实现插值计算逻辑与对应 UI（标记关键帧、查看插值结果）

- [x] **扩展导出格式以支持视频标注结果**
  - [x] 评估现有导出格式（COCO/VOC/YOLO）是否有视频标注扩展惯例，或设计自定义结构
  - [x] 实现对应导出逻辑并验证

- [x] **视频标注导出增强**
  - [x] 项目设置里增加默认抽帧设置
  - [x] 增加按默认设置对项目中所有未抽帧视频进行抽帧的功能
  - [x] 在文件列表中，对视频右键时，可以对此视频重新抽帧，此时原来已抽帧的图片和标注会删除，需要复用删除提醒进行警告
  - [x] 增加YOLO txt导出支持，按对抽帧的图片进行标注文件导出

- [x] **前端交互①：遮罩模块与输入门禁**
  - [x] 按 `docs/frontend-interaction.md` §1 实现 Overlay 模块：组件挂载自注册进栈、z-index 由栈深度推导、仅栈顶响应 Esc、焦点进入与归还、视口高度上限与内部滚动槽（ADR 0008）
  - [x] 迁移 17 个遮罩：菜单与搜索浮层为轻量级，其余为阻塞级；删除各文件的 `fixed inset-0` 背板与 50/60/65/85/90/95/100 七个 z-index 值
  - [x] 删除 `App.tsx` 的 9 项门禁与运算，键盘消费者改读 `hasBlocking()` / `hasLight()`
  - [x] 新增目录与 store 归属同步更新 `AGENTS.md` §4
  - 验收：320px 高窗口下关闭控件可达；Esc 只关闭栈顶、删除进行中不可关闭；菜单或搜索浮层打开时画布标注不被快捷键改动

- [x] **前端交互②：快捷键动作表与统一解析**
  - [x] 按 `docs/frontend-interaction.md` §2 建立动作元数据表（id / 名称 / 默认键位 / scope / 是否可改绑）与纯函数 `resolveShortcut(event, ctx)`
  - [x] 功能模块经 `useShortcut(id, handler)` 注册行为，未注册即「不可用」并在提示浮层置灰
  - [x] 固定键（`Ctrl+Z/Y/S`、删除标注框的 `Delete`）进表并标记不可改绑，设置面板作为固定行展示
  - [x] 冲突判定收敛为一份 `detectConflicts`，供设置面板、标签编辑器与运行时共用；`mergeShortcuts` 的新默认键例外由表推导
  - 验收：四层优先级（blocking / light / canvas / global）与冲突裁决可纯函数验证；新增动作不再修改 `App.tsx` 与 `AppLayout.tsx`

- [x] **前端交互③：操作注册表与按资源互斥**
  - [x] 按 `docs/frontend-interaction.md` §3 实现注册表：`begin` / `progress` / `complete` / `fail` / `canStart(resource)`，操作声明占用资源与取消句柄（ADR 0010）
  - [x] 迁移预打标、插件导出、视频导入与抽帧、模型与运行时下载、保存、图片删除到注册表，界面退化为薄适配器
  - [x] 消息按操作归属并带类型，成功不再借用错误通道；更新下载补上取消入口
  - [x] 删除 `workspaceDisabled`、键盘门禁与 `canDeleteImage` 中的重复与运算，以及写三遍的视频导入锁表达式
  - 验收：并发操作各自呈现进度与取消、消息不互相覆盖；占用同一资源的操作无法同时启动

- [x] **前端交互④：选择与作用域归属 store**
  - [x] 按 `docs/frontend-interaction.md` §4 在 `useAnnotationStore` 增加选择切片：`select` / `pushScope` / `popScope` / `selectAdjacent` / `selectUnannotated` / `removeImages`（ADR 0009）
  - [x] 删除与接续、标注清理、历史裁剪合并为一次原子更新，修掉删除当前图片后选择被清空（画布变白）的路径
  - [x] 侧栏增加常驻可见、可退出的作用域条；删除 `ProjectMediaList` 的 `remembered` 影子游标与搜索对话框的私有游标
  - [x] 列表行模块自行注册滚入视口，替换 App 的 `selectedImageButtonRef` 约定
  - [x] 修订 `AGENTS.md` §6 的分层措辞（瞬时交互态与当前编辑对象的边界）
  - 验收：搜索确认后「下一张」留在结果集内；删除任意一张（含末张、含当前未在列表中的路径）后选择停在邻图；作用域切换可见可退出

- [x] **前端交互⑤：画布手势意图与草稿状态机**
  - [x] 按 `docs/frontend-interaction.md` §5 实现纯函数 `resolveGesture`，Stage 统一分类，Konva 保留拖拽与 Transformer
  - [x] 三个图形渲染器删除各自的按钮 / 模式守卫，改为向同一解析器查询
  - [x] 草稿状态机 `idle | rect | polygon | point | pan` 统一提交与取消，矩形绘制中支持 Esc 取消
  - [x] 坐标转换收成 `createTransform(layout)` adapter，替换画布交互、缩放、多边形草稿与插值预览中各自重推的反向变换
  - 验收：分类矩阵（按钮 × 模式 × 命中/背景）与草稿转移可纯函数验证；矩形与多边形的取消行为一致

- [x] **前端交互⑥：视频帧模型与导航接缝**
  - [x] 按 `docs/frontend-interaction.md` §6 实现纯模型 `lib/video-frames.ts`：帧序、每帧是否已标注、关键帧集合、帧 ↔ 路径映射
  - [x] 提供 `useVideoFrameNavigation`（当前帧 / 步进 / 跳转 / 边界），时间轴、侧栏帧列表与键盘步进共用
  - [x] `VideoTimeline` 渲染标注密度、关键帧标记与播放头，回调仅保留 `onSelectFrame`
  - 验收：时间轴与侧栏显示一致的标注分布；帧步进与跳转在首尾边界行为一致；删除以字符串名字互相翻译的两个索引空间
